import {
  type AnyPgColumn,
  bigint,
  check,
  date,
  doublePrecision,
  foreignKey,
  index,
  integer,
  jsonb,
  numeric,
  pgTable,
  primaryKey,
  text,
  timestamp,
  uniqueIndex,
} from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";

const tenantWorkspaceDefault = sql`NULLIF(current_setting('app.workspace_id', true), '')`;

export const users = pgTable("fdp_users", {
  id: text("id").primaryKey(),
  email: text("email").notNull(),
  name: text("name").notNull(),
  passwordHash: text("password_hash"),
  passwordSalt: text("password_salt"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_users_email_uq").on(table.email),
  index("fdp_users_created_id_idx").on(table.createdAt, table.id),
]);

export const workspaces = pgTable("fdp_workspaces", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  slug: text("slug").notNull(),
  ownerUserId: text("owner_user_id").notNull().references(() => users.id),
  timezone: text("timezone").notNull().default("America/Sao_Paulo"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_workspaces_owner_idx").on(table.ownerUserId),
  index("fdp_workspaces_created_id_idx").on(table.createdAt, table.id),
  uniqueIndex("fdp_workspaces_slug_uq").on(table.slug),
]);

export const companies = pgTable("fdp_companies", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  parentCompanyId: text("parent_company_id").references((): AnyPgColumn => companies.id, { onDelete: "set null" }),
  isPrincipal: integer("is_principal").notNull().default(0),
  legalName: text("legal_name").notNull(),
  tradeName: text("trade_name").notNull().default(""),
  taxId: text("tax_id").notNull().default(""),
  externalCode: text("external_code").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  taxRegime: text("tax_regime").notNull().default(""),
  stateRegistration: text("state_registration").notNull().default(""),
  municipalRegistration: text("municipal_registration").notNull().default(""),
  postalCode: text("postal_code").notNull().default(""),
  street: text("street").notNull().default(""),
  streetNumber: text("street_number").notNull().default(""),
  addressComplement: text("address_complement").notNull().default(""),
  district: text("district").notNull().default(""),
  city: text("city").notNull().default(""),
  state: text("state").notNull().default(""),
  country: text("country").notNull().default("Brasil"),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_companies_workspace_name_idx").on(table.workspaceId, table.legalName),
  index("fdp_companies_workspace_tax_idx").on(table.workspaceId, table.taxId),
  index("fdp_companies_workspace_parent_idx").on(table.workspaceId, table.parentCompanyId),
  uniqueIndex("fdp_companies_workspace_id_uq").on(table.workspaceId, table.id),
  foreignKey({
    name: "fdp_companies_workspace_parent_fk",
    columns: [table.workspaceId, table.parentCompanyId],
    foreignColumns: [table.workspaceId, table.id],
  }),
]);

export const workspaceMembers = pgTable("fdp_workspace_members", {
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  role: text("role").notNull().default("admin"),
  joinedAt: timestamp("joined_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [primaryKey({ columns: [table.workspaceId, table.userId] })]);

export const memberCompanyAccess = pgTable("fdp_member_company_access", {
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  primaryKey({ columns: [table.workspaceId, table.userId, table.companyId] }),
  index("fdp_member_company_access_user_idx").on(table.workspaceId, table.userId),
  foreignKey({
    name: "fdp_member_company_access_member_fk",
    columns: [table.workspaceId, table.userId],
    foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_member_company_access_company_fk",
    columns: [table.workspaceId, table.companyId],
    foreignColumns: [companies.workspaceId, companies.id],
  }).onDelete("cascade"),
]);

export const accessRecoveryTokens = pgTable("fdp_access_recovery_tokens", {
  id: text("id").primaryKey(),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  tokenHash: text("token_hash").notNull(),
  createdBy: text("created_by").notNull().default("system"),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }).notNull(),
  usedAt: timestamp("used_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_access_recovery_token_hash_uq").on(table.tokenHash),
  index("fdp_access_recovery_user_expiry_idx").on(table.userId, table.expiresAt),
]);

export const authSessions = pgTable("fdp_auth_sessions", {
  id: text("id").primaryKey(),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  tokenHash: text("token_hash").notNull(),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }).notNull(),
  revokedAt: timestamp("revoked_at", { withTimezone: true, mode: "string" }),
  deviceLabel: text("device_label").notNull().default("Dispositivo desconhecido"),
  ipHash: text("ip_hash").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  lastSeenAt: timestamp("last_seen_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_auth_sessions_token_hash_uq").on(table.tokenHash),
  index("fdp_auth_sessions_user_expiry_idx").on(table.userId, table.expiresAt),
  index("fdp_auth_sessions_active_idx").on(table.expiresAt, table.revokedAt),
]);

export const authRateLimits = pgTable("fdp_auth_rate_limits", {
  keyHash: text("key_hash").primaryKey(),
  failureCount: integer("failure_count").notNull().default(0),
  windowStartedAt: timestamp("window_started_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  blockedUntil: timestamp("blocked_until", { withTimezone: true, mode: "string" }),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [index("fdp_auth_rate_limits_blocked_idx").on(table.blockedUntil)]);

export const bootstrapGuard = pgTable("fdp_bootstrap_guard", {
  id: integer("id").primaryKey(),
  workspaceId: text("workspace_id").references(() => workspaces.id, { onDelete: "restrict" }),
  claimedAt: timestamp("claimed_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
});

export const userWorkspacePreferences = pgTable("fdp_user_workspace_preferences", {
  userId: text("user_id").primaryKey().references(() => users.id, { onDelete: "cascade" }),
  activeWorkspaceId: text("active_workspace_id").references(() => workspaces.id, { onDelete: "set null" }),
  activeBoardId: text("active_board_id"),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
});

export const boards = pgTable("fdp_boards", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  description: text("description").notNull().default(""),
  boardType: text("board_type").notNull().default("general"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_boards_workspace_name_uq").on(table.workspaceId, table.name),
  uniqueIndex("fdp_boards_workspace_id_uq").on(table.workspaceId, table.id),
]);

export const lists = pgTable("fdp_lists", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  boardId: text("board_id").notNull().references(() => boards.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  kind: text("kind").notNull(),
  position: doublePrecision("position").notNull(),
  slaBehavior: text("sla_behavior").notNull().default("running"),
}, (table) => [
  uniqueIndex("fdp_lists_board_kind_uq").on(table.boardId, table.kind),
  uniqueIndex("fdp_lists_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_lists_workspace_board_id_uq").on(table.workspaceId, table.boardId, table.id),
  index("fdp_lists_board_position_idx").on(table.boardId, table.position),
  foreignKey({
    name: "fdp_lists_workspace_board_fk",
    columns: [table.workspaceId, table.boardId],
    foreignColumns: [boards.workspaceId, boards.id],
  }).onDelete("cascade"),
]);

/** Áreas operacionais cruzam empresas; departamentos abaixo continuam sendo lotação por CNPJ. */
export const areas = pgTable("fdp_areas", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  code: text("code").notNull(),
  description: text("description").notNull().default(""),
  status: text("status").notNull().default("active"),
  managerUserId: text("manager_user_id"),
  color: text("color").notNull().default("#475569"),
  icon: text("icon").notNull().default("building-2"),
  defaultSlaDays: integer("default_sla_days").notNull().default(3),
  settingsJson: jsonb("settings_json").$type<Record<string, unknown>>().notNull().default({}),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  archivedAt: timestamp("archived_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_areas_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_areas_workspace_code_uq").on(table.workspaceId, table.code),
  index("fdp_areas_workspace_status_name_idx").on(table.workspaceId, table.status, table.name),
  foreignKey({ name: "fdp_areas_manager_fk", columns: [table.workspaceId, table.managerUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_areas_status_check", sql`${table.status} IN ('active', 'inactive', 'archived')`),
]);

export const areaMembers = pgTable("fdp_area_members", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  areaId: text("area_id").notNull(),
  userId: text("user_id").notNull(),
  role: text("role").notNull().default("member"),
  isPrimary: integer("is_primary").notNull().default(0),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_area_members_workspace_area_user_uq").on(table.workspaceId, table.areaId, table.userId),
  uniqueIndex("fdp_area_members_workspace_user_primary_uq").on(table.workspaceId, table.userId).where(sql`${table.isPrimary} = 1`),
  index("fdp_area_members_workspace_user_idx").on(table.workspaceId, table.userId, table.areaId),
  foreignKey({ name: "fdp_area_members_area_fk", columns: [table.workspaceId, table.areaId], foreignColumns: [areas.workspaceId, areas.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_area_members_member_fk", columns: [table.workspaceId, table.userId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }).onDelete("cascade"),
  check("fdp_area_members_role_check", sql`${table.role} IN ('manager', 'member', 'observer')`),
]);

export const areaModuleAssignments = pgTable("fdp_area_module_assignments", {
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  moduleKey: text("module_key").notNull(),
  areaId: text("area_id").notNull(),
  createdBy: text("created_by").notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  primaryKey({ columns: [table.workspaceId, table.moduleKey] }),
  index("fdp_area_module_assignments_workspace_area_idx").on(table.workspaceId, table.areaId),
  foreignKey({ name: "fdp_area_module_assignments_area_fk", columns: [table.workspaceId, table.areaId], foreignColumns: [areas.workspaceId, areas.id] }).onDelete("cascade"),
]);

export const cards = pgTable("fdp_cards", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  boardId: text("board_id").notNull().references(() => boards.id, { onDelete: "cascade" }),
  listId: text("list_id").notNull().references(() => lists.id),
  title: text("title").notNull(),
  description: text("description").notNull().default(""),
  companyId: text("company_id").references(() => companies.id, { onDelete: "set null" }),
  company: text("company").notNull().default(""),
  requesterAreaId: text("requester_area_id"),
  responsibleAreaId: text("responsible_area_id"),
  processType: text("process_type").notNull().default("OUTROS"),
  priority: text("priority").notNull().default("normal"),
  assigneeName: text("assignee_name").notNull().default(""),
  dueAt: timestamp("due_at", { withTimezone: true, mode: "string" }),
  slaStatus: text("sla_status").notNull().default("safe"),
  position: doublePrecision("position").notNull(),
  sourceType: text("source_type").notNull().default("manual"),
  archived: integer("archived").notNull().default(0),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  slaTargetMinutes: integer("sla_target_minutes").notNull().default(0),
  slaStartedAt: timestamp("sla_started_at", { withTimezone: true, mode: "string" }),
  slaPausedMinutes: integer("sla_paused_minutes").notNull().default(0),
  slaPauseReason: text("sla_pause_reason").notNull().default(""),
  slaEscalationLevel: integer("sla_escalation_level").notNull().default(0),
  competence: text("competence").notNull().default(""),
  legalDueAt: timestamp("legal_due_at", { withTimezone: true, mode: "string" }),
  processTemplateId: text("process_template_id"),
  closedAt: timestamp("closed_at", { withTimezone: true, mode: "string" }),
}, (table) => [
  uniqueIndex("fdp_cards_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_cards_board_list_position_idx").on(table.boardId, table.listId, table.position),
  index("fdp_cards_due_status_idx").on(table.dueAt, table.slaStatus),
  foreignKey({
    name: "fdp_cards_workspace_board_list_fk",
    columns: [table.workspaceId, table.boardId, table.listId],
    foreignColumns: [lists.workspaceId, lists.boardId, lists.id],
  }),
  check("fdp_cards_competence_check", sql`${table.competence} = '' OR ${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  foreignKey({
    name: "fdp_cards_workspace_company_fk",
    columns: [table.workspaceId, table.companyId],
    foreignColumns: [companies.workspaceId, companies.id],
  }),
  foreignKey({ name: "fdp_cards_requester_area_fk", columns: [table.workspaceId, table.requesterAreaId], foreignColumns: [areas.workspaceId, areas.id] }),
  foreignKey({ name: "fdp_cards_responsible_area_fk", columns: [table.workspaceId, table.responsibleAreaId], foreignColumns: [areas.workspaceId, areas.id] }),
  index("fdp_cards_workspace_requester_area_idx").on(table.workspaceId, table.requesterAreaId, table.createdAt),
  index("fdp_cards_workspace_responsible_area_idx").on(table.workspaceId, table.responsibleAreaId, table.createdAt),
]);

export const checklistItems = pgTable("fdp_checklist_items", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  title: text("title").notNull(),
  completed: integer("completed").notNull().default(0),
  position: doublePrecision("position").notNull(),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
}, (table) => [
  index("fdp_checklist_card_position_idx").on(table.cardId, table.position),
  foreignKey({
    name: "fdp_checklist_items_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
]);

export const cardComments = pgTable("fdp_card_comments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  authorUserId: text("author_user_id").notNull().references(() => users.id),
  body: text("body").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_comments_card_created_idx").on(table.cardId, table.createdAt),
  foreignKey({
    name: "fdp_card_comments_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
]);

export const labels = pgTable("fdp_labels", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  color: text("color").notNull().default("#64748b"),
  position: doublePrecision("position").notNull(),
}, (table) => [
  uniqueIndex("fdp_labels_workspace_name_uq").on(table.workspaceId, table.name),
  uniqueIndex("fdp_labels_workspace_id_uq").on(table.workspaceId, table.id),
]);

export const cardLabels = pgTable("fdp_card_labels", {
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  labelId: text("label_id").notNull().references(() => labels.id, { onDelete: "cascade" }),
}, (table) => [
  primaryKey({ columns: [table.cardId, table.labelId] }),
  foreignKey({
    name: "fdp_card_labels_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_card_labels_workspace_label_fk",
    columns: [table.workspaceId, table.labelId],
    foreignColumns: [labels.workspaceId, labels.id],
  }).onDelete("cascade"),
]);

export const cardAssignees = pgTable("fdp_card_assignees", {
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
}, (table) => [
  primaryKey({ columns: [table.cardId, table.userId] }),
  foreignKey({
    name: "fdp_card_assignees_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_card_assignees_workspace_member_fk",
    columns: [table.workspaceId, table.userId],
    foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId],
  }).onDelete("cascade"),
]);

export const customFields = pgTable("fdp_custom_fields", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  fieldKey: text("field_key").notNull(),
  fieldType: text("field_type").notNull().default("text"),
  optionsJson: text("options_json").notNull().default("[]"),
  required: integer("required").notNull().default(0),
  position: doublePrecision("position").notNull(),
}, (table) => [
  uniqueIndex("fdp_custom_fields_workspace_key_uq").on(table.workspaceId, table.fieldKey),
  uniqueIndex("fdp_custom_fields_workspace_id_uq").on(table.workspaceId, table.id),
]);

export const customFieldValues = pgTable("fdp_custom_field_values", {
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  fieldId: text("field_id").notNull().references(() => customFields.id, { onDelete: "cascade" }),
  valueText: text("value_text").notNull().default(""),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  primaryKey({ columns: [table.cardId, table.fieldId] }),
  foreignKey({
    name: "fdp_custom_field_values_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_custom_field_values_workspace_field_fk",
    columns: [table.workspaceId, table.fieldId],
    foreignColumns: [customFields.workspaceId, customFields.id],
  }).onDelete("cascade"),
]);

export const cardAttachments = pgTable("fdp_card_attachments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  objectKey: text("object_key").notNull(),
  filename: text("filename").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: integer("size_bytes").notNull(),
  uploadedBy: text("uploaded_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_attachments_object_key_uq").on(table.objectKey),
  index("fdp_attachments_card_created_idx").on(table.cardId, table.createdAt),
  foreignKey({
    name: "fdp_card_attachments_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
]);

export const processTemplates = pgTable("fdp_process_templates", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  processType: text("process_type").notNull(),
  description: text("description").notNull().default(""),
  checklistJson: text("checklist_json").notNull().default("[]"),
  defaultSlaDays: integer("default_sla_days").notNull().default(3),
  active: integer("active").notNull().default(1),
  position: doublePrecision("position").notNull(),
}, (table) => [
  uniqueIndex("fdp_templates_workspace_name_uq").on(table.workspaceId, table.name),
  uniqueIndex("fdp_templates_workspace_id_uq").on(table.workspaceId, table.id),
]);

export const workspaceSettings = pgTable("fdp_workspace_settings", {
  workspaceId: text("workspace_id").primaryKey().references(() => workspaces.id, { onDelete: "cascade" }),
  businessDaysJson: text("business_days_json").notNull().default("[1,2,3,4,5]"),
  dayStart: text("day_start").notNull().default("08:00"),
  dayEnd: text("day_end").notNull().default("18:00"),
  realtimeSeconds: integer("realtime_seconds").notNull().default(30),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
});

export const businessHolidays = pgTable("fdp_business_holidays", {
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  holidayDate: date("holiday_date", { mode: "string" }).notNull(),
  name: text("name").notNull(),
}, (table) => [primaryKey({ columns: [table.workspaceId, table.holidayDate] })]);

export const slaPolicies = pgTable("fdp_sla_policies", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  processType: text("process_type").notNull(),
  targetBusinessDays: integer("target_business_days").notNull().default(3),
  warningBusinessDays: integer("warning_business_days").notNull().default(1),
  active: integer("active").notNull().default(1),
}, (table) => [uniqueIndex("fdp_sla_policies_workspace_process_uq").on(table.workspaceId, table.processType)]);

export const notifications = pgTable("fdp_notifications", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  eventKey: text("event_key").notNull(),
  notificationType: text("notification_type").notNull(),
  title: text("title").notNull(),
  body: text("body").notNull().default(""),
  cardId: text("card_id").references(() => cards.id, { onDelete: "cascade" }),
  readAt: timestamp("read_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_notifications_user_event_uq").on(table.userId, table.eventKey),
  index("fdp_notifications_user_read_created_idx").on(table.userId, table.readAt, table.createdAt),
  foreignKey({
    name: "fdp_notifications_workspace_member_fk",
    columns: [table.workspaceId, table.userId],
    foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_notifications_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }),
]);

export const integrations = pgTable("fdp_integrations", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  channel: text("channel").notNull(),
  displayName: text("display_name").notNull(),
  status: text("status").notNull().default("needs_credentials"),
  configJson: text("config_json").notNull().default("{}"),
  lastSyncAt: timestamp("last_sync_at", { withTimezone: true, mode: "string" }),
  lastConnectionAt: timestamp("last_connection_at", { withTimezone: true, mode: "string" }),
  lastSuccessfulSyncAt: timestamp("last_successful_sync_at", { withTimezone: true, mode: "string" }),
  nextSyncAt: timestamp("next_sync_at", { withTimezone: true, mode: "string" }),
  scheduleEnabled: integer("schedule_enabled").notNull().default(0),
  connectorVersion: text("connector_version").notNull().default(""),
  lastError: text("last_error"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integrations_workspace_channel_uq").on(table.workspaceId, table.channel),
  uniqueIndex("fdp_integrations_workspace_id_uq").on(table.workspaceId, table.id),
]);

export const plannerBlocks = pgTable("fdp_planner_blocks", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  cardId: text("card_id").references(() => cards.id, { onDelete: "set null" }),
  title: text("title").notNull(),
  startAt: timestamp("start_at", { withTimezone: true, mode: "string" }).notNull(),
  endAt: timestamp("end_at", { withTimezone: true, mode: "string" }).notNull(),
  blockType: text("block_type").notNull().default("focus"),
  notes: text("notes").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_planner_user_start_idx").on(table.userId, table.startAt),
  foreignKey({
    name: "fdp_planner_blocks_workspace_member_fk",
    columns: [table.workspaceId, table.userId],
    foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId],
  }).onDelete("cascade"),
  foreignKey({
    name: "fdp_planner_blocks_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }),
]);

export const calendarConnections = pgTable("fdp_calendar_connections", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  provider: text("provider").notNull(),
  status: text("status").notNull().default("needs_credentials"),
  configJson: text("config_json").notNull().default("{}"),
  externalCalendarId: text("external_calendar_id"),
  lastSyncAt: timestamp("last_sync_at", { withTimezone: true, mode: "string" }),
  lastError: text("last_error"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_calendar_connections_user_provider_uq").on(table.userId, table.provider),
  foreignKey({
    name: "fdp_calendar_connections_workspace_member_fk",
    columns: [table.workspaceId, table.userId],
    foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId],
  }).onDelete("cascade"),
]);

export const cardSlaPauses = pgTable("fdp_card_sla_pauses", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  cardId: text("card_id").notNull().references(() => cards.id, { onDelete: "cascade" }),
  startedAt: timestamp("started_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  endedAt: timestamp("ended_at", { withTimezone: true, mode: "string" }),
  reason: text("reason").notNull(),
  createdBy: text("created_by").notNull(),
}, (table) => [
  index("fdp_card_sla_pause_open_idx").on(table.cardId, table.endedAt),
  foreignKey({
    name: "fdp_card_sla_pauses_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
]);

export const inboxItems = pgTable("fdp_workspace_inbox_items", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  channel: text("channel").notNull().default("manual"),
  senderName: text("sender_name").notNull(),
  subject: text("subject").notNull(),
  body: text("body").notNull().default(""),
  status: text("status").notNull().default("new"),
  receivedAt: timestamp("received_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  convertedCardId: text("converted_card_id").references(() => cards.id),
}, (table) => [
  index("fdp_inbox_workspace_status_received_idx").on(table.workspaceId, table.status, table.receivedAt),
  foreignKey({
    name: "fdp_workspace_inbox_items_workspace_card_fk",
    columns: [table.workspaceId, table.convertedCardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }),
]);

export const automationRules = pgTable("fdp_automation_rules", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  trigger: text("trigger").notNull(),
  conditionJson: text("condition_json").notNull().default("{}"),
  actionJson: text("action_json").notNull().default("{}"),
  enabled: integer("enabled").notNull().default(1),
  position: doublePrecision("position").notNull(),
}, (table) => [index("fdp_rules_workspace_position_idx").on(table.workspaceId, table.position)]);

export const activityEvents = pgTable("fdp_activity_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  cardId: text("card_id").references(() => cards.id, { onDelete: "cascade" }),
  actorEmail: text("actor_email").notNull(),
  eventType: text("event_type").notNull(),
  payloadJson: text("payload_json").notNull().default("{}"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_activity_workspace_created_idx").on(table.workspaceId, table.createdAt),
  foreignKey({
    name: "fdp_activity_events_workspace_card_fk",
    columns: [table.workspaceId, table.cardId],
    foreignColumns: [cards.workspaceId, cards.id],
  }).onDelete("cascade"),
]);

export const auditEvents = pgTable("fdp_audit_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "restrict" }),
  actorType: text("actor_type").notNull().default("user"),
  actorUserId: text("actor_user_id").references(() => users.id, { onDelete: "set null" }),
  actorEmail: text("actor_email").notNull(),
  action: text("action").notNull(),
  outcome: text("outcome").notNull().default("success"),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id"),
  beforeJson: jsonb("before_json").$type<Record<string, unknown>>().notNull().default({}),
  afterJson: jsonb("after_json").$type<Record<string, unknown>>().notNull().default({}),
  metadataJson: jsonb("metadata_json").$type<Record<string, unknown>>().notNull().default({}),
  requestId: text("request_id"),
  ipHash: text("ip_hash"),
  userAgentHash: text("user_agent_hash"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_audit_workspace_created_idx").on(table.workspaceId, table.createdAt),
  index("fdp_audit_workspace_entity_idx").on(table.workspaceId, table.entityType, table.entityId),
  index("fdp_audit_workspace_actor_idx").on(table.workspaceId, table.actorUserId, table.createdAt),
  check("fdp_audit_events_actor_type_check", sql`${table.actorType} IN ('user', 'system', 'integration')`),
  check("fdp_audit_events_outcome_check", sql`${table.outcome} IN ('success', 'denied', 'failure')`),
]);

export const departments = pgTable("fdp_departments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  parentDepartmentId: text("parent_department_id"),
  code: text("code").notNull(),
  name: text("name").notNull(),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_departments_workspace_company_code_uq").on(table.workspaceId, table.companyId, table.code),
  uniqueIndex("fdp_departments_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  foreignKey({ name: "fdp_departments_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_departments_workspace_parent_fk", columns: [table.workspaceId, table.companyId, table.parentDepartmentId], foreignColumns: [table.workspaceId, table.companyId, table.id] }),
  check("fdp_departments_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const positions = pgTable("fdp_positions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  code: text("code").notNull(),
  name: text("name").notNull(),
  cboCode: text("cbo_code").notNull().default(""),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_positions_workspace_company_code_uq").on(table.workspaceId, table.companyId, table.code),
  uniqueIndex("fdp_positions_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  foreignKey({ name: "fdp_positions_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  check("fdp_positions_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const costCenters = pgTable("fdp_cost_centers", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  code: text("code").notNull(),
  name: text("name").notNull(),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_cost_centers_workspace_company_code_uq").on(table.workspaceId, table.companyId, table.code),
  uniqueIndex("fdp_cost_centers_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  foreignKey({ name: "fdp_cost_centers_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  check("fdp_cost_centers_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const workSchedules = pgTable("fdp_work_schedules", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  code: text("code").notNull(),
  name: text("name").notNull(),
  weeklyHours: integer("weekly_hours").notNull().default(44),
  description: text("description").notNull().default(""),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_work_schedules_workspace_company_code_uq").on(table.workspaceId, table.companyId, table.code),
  uniqueIndex("fdp_work_schedules_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  foreignKey({ name: "fdp_work_schedules_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  check("fdp_work_schedules_weekly_hours_check", sql`${table.weeklyHours} BETWEEN 1 AND 60`),
  check("fdp_work_schedules_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const employees = pgTable("fdp_employees", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  departmentId: text("department_id"),
  positionId: text("position_id"),
  costCenterId: text("cost_center_id"),
  workScheduleId: text("work_schedule_id"),
  managerEmployeeId: text("manager_employee_id"),
  registrationNumber: text("registration_number").notNull(),
  fullName: text("full_name").notNull(),
  socialName: text("social_name").notNull().default(""),
  cpfHash: text("cpf_hash").notNull().default(""),
  cpfLast4: text("cpf_last4").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  birthDate: date("birth_date", { mode: "string" }),
  admissionDate: date("admission_date", { mode: "string" }).notNull(),
  terminationDate: date("termination_date", { mode: "string" }),
  employmentStatus: text("employment_status").notNull().default("active"),
  employmentType: text("employment_type").notNull().default("clt"),
  workModel: text("work_model").notNull().default("onsite"),
  sourceSystem: text("source_system").notNull().default("manual"),
  externalId: text("external_id").notNull().default(""),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_employees_workspace_company_registration_uq").on(table.workspaceId, table.companyId, table.registrationNumber),
  uniqueIndex("fdp_employees_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_employees_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  uniqueIndex("fdp_employees_workspace_cpf_hash_uq").on(table.workspaceId, table.cpfHash).where(sql`${table.cpfHash} <> ''`),
  index("fdp_employees_workspace_status_name_idx").on(table.workspaceId, table.employmentStatus, table.fullName),
  foreignKey({ name: "fdp_employees_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_employees_workspace_department_fk", columns: [table.workspaceId, table.companyId, table.departmentId], foreignColumns: [departments.workspaceId, departments.companyId, departments.id] }),
  foreignKey({ name: "fdp_employees_workspace_position_fk", columns: [table.workspaceId, table.companyId, table.positionId], foreignColumns: [positions.workspaceId, positions.companyId, positions.id] }),
  foreignKey({ name: "fdp_employees_workspace_cost_center_fk", columns: [table.workspaceId, table.companyId, table.costCenterId], foreignColumns: [costCenters.workspaceId, costCenters.companyId, costCenters.id] }),
  foreignKey({ name: "fdp_employees_workspace_work_schedule_fk", columns: [table.workspaceId, table.companyId, table.workScheduleId], foreignColumns: [workSchedules.workspaceId, workSchedules.companyId, workSchedules.id] }),
  foreignKey({ name: "fdp_employees_workspace_manager_fk", columns: [table.workspaceId, table.managerEmployeeId], foreignColumns: [table.workspaceId, table.id] }),
  check("fdp_employees_status_check", sql`${table.employmentStatus} IN ('active', 'on_leave', 'terminated')`),
  check("fdp_employees_type_check", sql`${table.employmentType} IN ('clt', 'intern', 'apprentice', 'temporary')`),
  check("fdp_employees_work_model_check", sql`${table.workModel} IN ('onsite', 'hybrid', 'remote')`),
  check("fdp_employees_source_check", sql`${table.sourceSystem} IN ('manual', 'solides', 'sankhya')`),
]);

export const payrollCycles = pgTable("fdp_payroll_cycles", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  competence: text("competence").notNull(),
  status: text("status").notNull().default("open"),
  preClosingDueDate: date("pre_closing_due_date", { mode: "string" }),
  paymentDate: date("payment_date", { mode: "string" }),
  postClosingDueDate: date("post_closing_due_date", { mode: "string" }),
  closedAt: timestamp("closed_at", { withTimezone: true, mode: "string" }),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_payroll_cycles_workspace_company_competence_uq").on(table.workspaceId, table.companyId, table.competence),
  uniqueIndex("fdp_payroll_cycles_workspace_company_id_uq").on(table.workspaceId, table.companyId, table.id),
  uniqueIndex("fdp_payroll_cycles_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_payroll_cycles_workspace_status_idx").on(table.workspaceId, table.status, table.competence),
  foreignKey({ name: "fdp_payroll_cycles_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  check("fdp_payroll_cycles_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_payroll_cycles_status_check", sql`${table.status} IN ('open', 'pre_closing', 'processing', 'post_closing', 'closed')`),
]);

export const employeeMovements = pgTable("fdp_employee_movements", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  employeeId: text("employee_id").notNull().references(() => employees.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id"),
  cardId: text("card_id"),
  movementType: text("movement_type").notNull(),
  effectiveDate: date("effective_date", { mode: "string" }).notNull(),
  title: text("title").notNull(),
  detailsJson: jsonb("details_json").$type<Record<string, unknown>>().notNull().default({}),
  status: text("status").notNull().default("draft"),
  requestedBy: text("requested_by").notNull(),
  decidedBy: text("decided_by"),
  decidedAt: timestamp("decided_at", { withTimezone: true, mode: "string" }),
  decisionComment: text("decision_comment").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_employee_movements_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_employee_movements_workspace_status_effective_idx").on(table.workspaceId, table.status, table.effectiveDate),
  index("fdp_employee_movements_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.effectiveDate),
  foreignKey({ name: "fdp_employee_movements_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_employee_movements_workspace_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_employee_movements_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_employee_movements_workspace_card_fk", columns: [table.workspaceId, table.cardId], foreignColumns: [cards.workspaceId, cards.id] }),
  foreignKey({ name: "fdp_employee_movements_workspace_requester_fk", columns: [table.workspaceId, table.requestedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_employee_movements_workspace_decider_fk", columns: [table.workspaceId, table.decidedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_employee_movements_type_check", sql`${table.movementType} IN ('salary_change', 'vacation', 'leave', 'termination', 'transfer', 'benefit_change', 'registration_sync', 'epi_discount', 'other')`),
  check("fdp_employee_movements_status_check", sql`${table.status} IN ('draft', 'pending_approval', 'approved', 'rejected', 'applied', 'canceled')`),
]);

export const movementApprovalSteps = pgTable("fdp_movement_approval_steps", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  movementId: text("movement_id").notNull().references(() => employeeMovements.id, { onDelete: "cascade" }),
  sequence: integer("sequence").notNull().default(1),
  approverUserId: text("approver_user_id").notNull(),
  status: text("status").notNull().default("pending"),
  decidedAt: timestamp("decided_at", { withTimezone: true, mode: "string" }),
  comment: text("comment").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_movement_approval_steps_movement_sequence_uq").on(table.movementId, table.sequence),
  index("fdp_movement_approval_steps_workspace_approver_idx").on(table.workspaceId, table.approverUserId, table.status),
  foreignKey({ name: "fdp_movement_approval_steps_workspace_movement_fk", columns: [table.workspaceId, table.movementId], foreignColumns: [employeeMovements.workspaceId, employeeMovements.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_movement_approval_steps_workspace_approver_fk", columns: [table.workspaceId, table.approverUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_movement_approval_steps_status_check", sql`${table.status} IN ('pending', 'approved', 'rejected', 'skipped')`),
  check("fdp_movement_approval_steps_sequence_check", sql`${table.sequence} > 0`),
]);

export const payrollCycleItems = pgTable("fdp_payroll_cycle_items", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "cascade" }),
  phase: text("phase").notNull(),
  category: text("category").notNull().default("general"),
  title: text("title").notNull(),
  status: text("status").notNull().default("pending"),
  ownerUserId: text("owner_user_id"),
  dueDate: date("due_date", { mode: "string" }),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
  notes: text("notes").notNull().default(""),
  position: doublePrecision("position").notNull().default(1000),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_payroll_cycle_items_cycle_phase_title_uq").on(table.payrollCycleId, table.phase, table.title),
  uniqueIndex("fdp_payroll_cycle_items_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_payroll_cycle_items_workspace_status_idx").on(table.workspaceId, table.status, table.dueDate),
  foreignKey({ name: "fdp_payroll_cycle_items_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_payroll_cycle_items_workspace_owner_fk", columns: [table.workspaceId, table.ownerUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_payroll_cycle_items_phase_check", sql`${table.phase} IN ('pre_closing', 'post_closing')`),
  check("fdp_payroll_cycle_items_status_check", sql`${table.status} IN ('pending', 'in_progress', 'blocked', 'completed')`),
]);

export const complianceObligations = pgTable("fdp_compliance_obligations", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id"),
  cardId: text("card_id"),
  obligationType: text("obligation_type").notNull().default("other"),
  title: text("title").notNull(),
  dueDate: date("due_date", { mode: "string" }).notNull(),
  status: text("status").notNull().default("open"),
  ownerUserId: text("owner_user_id"),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
  recurrenceKey: text("recurrence_key").notNull().default(""),
  notes: text("notes").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_compliance_obligations_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_compliance_obligations_workspace_recurrence_uq").on(table.workspaceId, table.recurrenceKey).where(sql`${table.recurrenceKey} <> ''`),
  index("fdp_compliance_obligations_workspace_due_status_idx").on(table.workspaceId, table.dueDate, table.status),
  foreignKey({ name: "fdp_compliance_obligations_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_compliance_obligations_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_compliance_obligations_workspace_card_fk", columns: [table.workspaceId, table.cardId], foreignColumns: [cards.workspaceId, cards.id] }),
  foreignKey({ name: "fdp_compliance_obligations_workspace_owner_fk", columns: [table.workspaceId, table.ownerUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_compliance_obligations_type_check", sql`${table.obligationType} IN ('payroll', 'social_security', 'tax', 'reporting', 'union', 'other')`),
  check("fdp_compliance_obligations_status_check", sql`${table.status} IN ('open', 'in_progress', 'blocked', 'completed')`),
]);

export const processDefinitions = pgTable("fdp_process_definitions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  code: text("code").notNull(),
  name: text("name").notNull(),
  category: text("category").notNull().default("general"),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_process_definitions_workspace_code_uq").on(table.workspaceId, table.code),
  uniqueIndex("fdp_process_definitions_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_process_definitions_workspace_status_idx").on(table.workspaceId, table.status, table.name),
  check("fdp_process_definitions_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const processVersions = pgTable("fdp_process_versions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  definitionId: text("definition_id").notNull().references(() => processDefinitions.id, { onDelete: "cascade" }),
  version: integer("version").notNull(),
  status: text("status").notNull().default("draft"),
  configurationJson: jsonb("configuration_json").$type<Record<string, unknown>>().notNull().default({}),
  publishedAt: timestamp("published_at", { withTimezone: true, mode: "string" }),
  publishedBy: text("published_by"),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_process_versions_definition_version_uq").on(table.definitionId, table.version),
  uniqueIndex("fdp_process_versions_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_process_versions_workspace_status_idx").on(table.workspaceId, table.status, table.createdAt),
  foreignKey({ name: "fdp_process_versions_workspace_definition_fk", columns: [table.workspaceId, table.definitionId], foreignColumns: [processDefinitions.workspaceId, processDefinitions.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_process_versions_workspace_publisher_fk", columns: [table.workspaceId, table.publishedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_process_versions_workspace_creator_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_process_versions_version_check", sql`${table.version} > 0`),
  check("fdp_process_versions_status_check", sql`${table.status} IN ('draft', 'published', 'retired')`),
]);

export const operationalPendingItems = pgTable("fdp_operational_pending_items", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id"),
  sourceType: text("source_type").notNull(),
  sourceId: text("source_id").notNull(),
  severity: text("severity").notNull().default("warning"),
  blocking: integer("blocking").notNull().default(0),
  title: text("title").notNull(),
  dueDate: date("due_date", { mode: "string" }),
  status: text("status").notNull().default("open"),
  ownerUserId: text("owner_user_id"),
  resolution: text("resolution").notNull().default(""),
  idempotencyKey: text("idempotency_key").notNull(),
  resolvedAt: timestamp("resolved_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_operational_pending_items_workspace_key_uq").on(table.workspaceId, table.idempotencyKey),
  uniqueIndex("fdp_operational_pending_items_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_operational_pending_items_workspace_status_due_idx").on(table.workspaceId, table.status, table.dueDate),
  foreignKey({ name: "fdp_operational_pending_items_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_operational_pending_items_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_operational_pending_items_workspace_owner_fk", columns: [table.workspaceId, table.ownerUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_operational_pending_items_source_check", sql`${table.sourceType} IN ('movement', 'approval', 'obligation', 'cycle', 'card')`),
  check("fdp_operational_pending_items_severity_check", sql`${table.severity} IN ('info', 'warning', 'critical')`),
  check("fdp_operational_pending_items_status_check", sql`${table.status} IN ('open', 'in_progress', 'resolved', 'waived')`),
  check("fdp_operational_pending_items_blocking_check", sql`${table.blocking} IN (0, 1)`),
]);

export const auxiliaryProviders = pgTable("fdp_auxiliary_providers", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  providerType: text("provider_type").notNull(),
  code: text("code").notNull(),
  legalName: text("legal_name").notNull(),
  tradeName: text("trade_name").notNull().default(""),
  taxId: text("tax_id").notNull().default(""),
  email: text("email").notNull().default(""),
  phone: text("phone").notNull().default(""),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_auxiliary_providers_workspace_code_uq").on(table.workspaceId, table.code),
  uniqueIndex("fdp_auxiliary_providers_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_auxiliary_providers_workspace_type_status_idx").on(table.workspaceId, table.providerType, table.status, table.legalName),
  check("fdp_auxiliary_providers_type_check", sql`${table.providerType} IN ('benefit_operator', 'psychologist', 'contractor')`),
  check("fdp_auxiliary_providers_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const auxiliaryExecutions = pgTable("fdp_auxiliary_executions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "restrict" }),
  providerId: text("provider_id"),
  moduleType: text("module_type").notNull(),
  referenceCode: text("reference_code").notNull(),
  title: text("title").notNull(),
  status: text("status").notNull().default("draft"),
  currentRevision: integer("current_revision").notNull().default(1),
  totalAmount: numeric("total_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  dueDate: date("due_date", { mode: "string" }),
  ownerUserId: text("owner_user_id"),
  closedBy: text("closed_by"),
  closedAt: timestamp("closed_at", { withTimezone: true, mode: "string" }),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_auxiliary_executions_workspace_reference_uq").on(table.workspaceId, table.referenceCode),
  uniqueIndex("fdp_auxiliary_executions_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_auxiliary_executions_workspace_cycle_module_idx").on(table.workspaceId, table.payrollCycleId, table.moduleType, table.status),
  index("fdp_auxiliary_executions_workspace_due_idx").on(table.workspaceId, table.status, table.dueDate),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_owner_fk", columns: [table.workspaceId, table.ownerUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_closer_fk", columns: [table.workspaceId, table.closedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_auxiliary_executions_workspace_creator_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_auxiliary_executions_module_check", sql`${table.moduleType} IN ('benefits', 'psychology', 'contractors')`),
  check("fdp_auxiliary_executions_status_check", sql`${table.status} IN ('draft', 'pending_approval', 'approved', 'rejected', 'closed', 'canceled')`),
  check("fdp_auxiliary_executions_revision_check", sql`${table.currentRevision} > 0`),
  check("fdp_auxiliary_executions_amount_check", sql`${table.totalAmount} >= 0`),
]);

export const auxiliaryExecutionRevisions = pgTable("fdp_auxiliary_execution_revisions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  executionId: text("execution_id").notNull().references(() => auxiliaryExecutions.id, { onDelete: "cascade" }),
  revision: integer("revision").notNull(),
  inputJson: jsonb("input_json").$type<Record<string, unknown>>().notNull().default({}),
  outputJson: jsonb("output_json").$type<Record<string, unknown>>().notNull().default({}),
  status: text("status").notNull().default("draft"),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_auxiliary_revisions_execution_revision_uq").on(table.executionId, table.revision),
  uniqueIndex("fdp_auxiliary_revisions_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_auxiliary_revisions_workspace_execution_id_uq").on(table.workspaceId, table.executionId, table.id),
  index("fdp_auxiliary_revisions_workspace_execution_idx").on(table.workspaceId, table.executionId, table.revision),
  foreignKey({ name: "fdp_auxiliary_revisions_workspace_execution_fk", columns: [table.workspaceId, table.executionId], foreignColumns: [auxiliaryExecutions.workspaceId, auxiliaryExecutions.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_auxiliary_revisions_workspace_creator_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_auxiliary_revisions_revision_check", sql`${table.revision} > 0`),
  check("fdp_auxiliary_revisions_status_check", sql`${table.status} IN ('draft', 'submitted', 'approved', 'rejected', 'superseded')`),
]);

export const auxiliaryApprovalSteps = pgTable("fdp_auxiliary_approval_steps", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  executionId: text("execution_id").notNull().references(() => auxiliaryExecutions.id, { onDelete: "cascade" }),
  revisionId: text("revision_id").notNull().references(() => auxiliaryExecutionRevisions.id, { onDelete: "cascade" }),
  sequence: integer("sequence").notNull().default(1),
  approverUserId: text("approver_user_id").notNull(),
  status: text("status").notNull().default("pending"),
  comment: text("comment").notNull().default(""),
  decidedAt: timestamp("decided_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_auxiliary_approval_revision_sequence_uq").on(table.revisionId, table.sequence),
  uniqueIndex("fdp_auxiliary_approval_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_auxiliary_approval_workspace_approver_idx").on(table.workspaceId, table.approverUserId, table.status),
  foreignKey({ name: "fdp_auxiliary_approval_workspace_execution_fk", columns: [table.workspaceId, table.executionId], foreignColumns: [auxiliaryExecutions.workspaceId, auxiliaryExecutions.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_auxiliary_approval_workspace_revision_fk", columns: [table.workspaceId, table.revisionId], foreignColumns: [auxiliaryExecutionRevisions.workspaceId, auxiliaryExecutionRevisions.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_auxiliary_approval_execution_revision_fk", columns: [table.workspaceId, table.executionId, table.revisionId], foreignColumns: [auxiliaryExecutionRevisions.workspaceId, auxiliaryExecutionRevisions.executionId, auxiliaryExecutionRevisions.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_auxiliary_approval_workspace_approver_fk", columns: [table.workspaceId, table.approverUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_auxiliary_approval_sequence_check", sql`${table.sequence} > 0`),
  check("fdp_auxiliary_approval_status_check", sql`${table.status} IN ('pending', 'approved', 'rejected', 'skipped')`),
]);

export const integrationCredentials = pgTable("fdp_integration_credentials", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  credentialType: text("credential_type").notNull().default("provider_auth"),
  encryptedValue: text("encrypted_value").notNull(),
  initializationVector: text("initialization_vector").notNull(),
  authTag: text("auth_tag").notNull(),
  keyVersion: integer("key_version").notNull().default(1),
  fingerprint: text("fingerprint").notNull(),
  publicHint: text("public_hint").notNull().default(""),
  status: text("status").notNull().default("active"),
  verifiedAt: timestamp("verified_at", { withTimezone: true, mode: "string" }),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }),
  rotatedAt: timestamp("rotated_at", { withTimezone: true, mode: "string" }),
  revokedAt: timestamp("revoked_at", { withTimezone: true, mode: "string" }),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_credentials_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_credentials_active_uq").on(table.workspaceId, table.integrationId, table.credentialType).where(sql`${table.status} = 'active'`),
  index("fdp_integration_credentials_workspace_integration_idx").on(table.workspaceId, table.integrationId, table.status),
  index("fdp_integration_credentials_expiry_idx").on(table.workspaceId, table.status, table.expiresAt),
  foreignKey({ name: "fdp_integration_credentials_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_credentials_workspace_creator_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_integration_credentials_type_check", sql`${table.credentialType} IN ('provider_auth', 'webhook_signing')`),
  check("fdp_integration_credentials_status_check", sql`${table.status} IN ('active', 'revoked')`),
  check("fdp_integration_credentials_key_version_check", sql`${table.keyVersion} > 0`),
]);

export const integrationMappings = pgTable("fdp_integration_mappings", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  resourceType: text("resource_type").notNull(),
  direction: text("direction").notNull().default("inbound"),
  version: integer("version").notNull().default(1),
  status: text("status").notNull().default("draft"),
  mappingJson: jsonb("mapping_json").$type<Record<string, unknown>>().notNull().default({}),
  checksum: text("checksum").notNull(),
  publishedAt: timestamp("published_at", { withTimezone: true, mode: "string" }),
  publishedBy: text("published_by"),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_mappings_integration_resource_version_uq").on(table.integrationId, table.resourceType, table.direction, table.version),
  uniqueIndex("fdp_integration_mappings_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_mappings_workspace_integration_id_uq").on(table.workspaceId, table.integrationId, table.id),
  index("fdp_integration_mappings_workspace_status_idx").on(table.workspaceId, table.integrationId, table.status),
  foreignKey({ name: "fdp_integration_mappings_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_mappings_workspace_creator_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_integration_mappings_workspace_publisher_fk", columns: [table.workspaceId, table.publishedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_integration_mappings_resource_check", sql`${table.resourceType} IN ('inbox', 'employees', 'hr_metrics', 'files', 'admissions')`),
  check("fdp_integration_mappings_direction_check", sql`${table.direction} IN ('inbound', 'outbound', 'bidirectional')`),
  check("fdp_integration_mappings_status_check", sql`${table.status} IN ('draft', 'active', 'archived')`),
  check("fdp_integration_mappings_version_check", sql`${table.version} > 0`),
]);

export const integrationSyncRuns = pgTable("fdp_integration_sync_runs", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  mappingId: text("mapping_id"),
  triggerType: text("trigger_type").notNull().default("manual"),
  status: text("status").notNull().default("queued"),
  idempotencyKey: text("idempotency_key").notNull(),
  cursor: text("cursor").notNull().default(""),
  attempt: integer("attempt").notNull().default(0),
  receivedCount: integer("received_count").notNull().default(0),
  processedCount: integer("processed_count").notNull().default(0),
  skippedCount: integer("skipped_count").notNull().default(0),
  conflictCount: integer("conflict_count").notNull().default(0),
  failedCount: integer("failed_count").notNull().default(0),
  updatedCount: integer("updated_count").notNull().default(0),
  unchangedCount: integer("unchanged_count").notNull().default(0),
  durationMs: integer("duration_ms").notNull().default(0),
  summary: text("summary").notNull().default(""),
  errorCode: text("error_code").notNull().default(""),
  errorMessage: text("error_message").notNull().default(""),
  requestedBy: text("requested_by"),
  startedAt: timestamp("started_at", { withTimezone: true, mode: "string" }),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_sync_runs_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_sync_runs_idempotency_uq").on(table.workspaceId, table.integrationId, table.idempotencyKey),
  uniqueIndex("fdp_integration_sync_runs_workspace_integration_id_uq").on(table.workspaceId, table.integrationId, table.id),
  uniqueIndex("fdp_sankhya_active_run_uq").on(table.workspaceId, table.integrationId)
    .where(sql`${table.mappingId} IS NULL AND ${table.status} IN ('queued', 'running', 'authenticating', 'navigating', 'processing', 'extracting', 'importing')`),
  index("fdp_integration_sync_runs_workspace_status_idx").on(table.workspaceId, table.status, table.createdAt),
  foreignKey({ name: "fdp_integration_sync_runs_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_sync_runs_workspace_mapping_fk", columns: [table.workspaceId, table.integrationId, table.mappingId], foreignColumns: [integrationMappings.workspaceId, integrationMappings.integrationId, integrationMappings.id] }),
  foreignKey({ name: "fdp_integration_sync_runs_workspace_requester_fk", columns: [table.workspaceId, table.requestedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_integration_sync_runs_trigger_check", sql`${table.triggerType} IN ('manual', 'scheduled', 'webhook', 'retry', 'health_check')`),
  check("fdp_integration_sync_runs_status_check", sql`${table.status} IN ('queued', 'running', 'authenticating', 'navigating', 'processing', 'extracting', 'importing', 'succeeded', 'partial', 'failed', 'requires_user_action', 'canceled')`),
  check("fdp_integration_sync_runs_counts_check", sql`${table.attempt} >= 0 AND ${table.receivedCount} >= 0 AND ${table.processedCount} >= 0 AND ${table.skippedCount} >= 0 AND ${table.conflictCount} >= 0 AND ${table.failedCount} >= 0 AND ${table.updatedCount} >= 0 AND ${table.unchangedCount} >= 0 AND ${table.durationMs} >= 0`),
]);

export const integrationRunLogs = pgTable("fdp_integration_run_logs", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  sequence: integer("sequence").notNull(),
  level: text("level").notNull().default("info"),
  phase: text("phase").notNull().default("queued"),
  code: text("code").notNull().default(""),
  message: text("message").notNull(),
  metadataJson: jsonb("metadata_json").$type<Record<string, unknown>>().notNull().default({}),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_run_logs_run_sequence_uq").on(table.runId, table.sequence),
  index("fdp_integration_run_logs_workspace_run_idx").on(table.workspaceId, table.runId, table.createdAt),
  foreignKey({ name: "fdp_integration_run_logs_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  check("fdp_integration_run_logs_level_check", sql`${table.level} IN ('info', 'warn', 'error')`),
]);

export const integrationDiagnostics = pgTable("fdp_integration_diagnostics", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  kind: text("kind").notNull().default("screenshot"),
  objectKey: text("object_key").notNull(),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }).notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_integration_diagnostics_workspace_run_idx").on(table.workspaceId, table.runId, table.expiresAt),
  foreignKey({ name: "fdp_integration_diagnostics_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  check("fdp_integration_diagnostics_kind_check", sql`${table.kind} IN ('screenshot')`),
]);

export const integrationSyncItems = pgTable("fdp_integration_sync_items", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  mappingId: text("mapping_id"),
  itemKey: text("item_key").notNull(),
  externalId: text("external_id").notNull().default(""),
  direction: text("direction").notNull().default("inbound"),
  status: text("status").notNull().default("pending"),
  payloadHash: text("payload_hash").notNull(),
  targetType: text("target_type").notNull().default(""),
  targetId: text("target_id").notNull().default(""),
  metadataJson: jsonb("metadata_json").$type<Record<string, unknown>>().notNull().default({}),
  errorCode: text("error_code").notNull().default(""),
  errorMessage: text("error_message").notNull().default(""),
  processedAt: timestamp("processed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_sync_items_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_sync_items_run_key_uq").on(table.runId, table.itemKey),
  uniqueIndex("fdp_integration_sync_items_payload_uq").on(table.workspaceId, table.integrationId, table.mappingId, table.externalId, table.payloadHash),
  index("fdp_integration_sync_items_workspace_run_status_idx").on(table.workspaceId, table.runId, table.status),
  foreignKey({ name: "fdp_integration_sync_items_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_sync_items_workspace_mapping_fk", columns: [table.workspaceId, table.integrationId, table.mappingId], foreignColumns: [integrationMappings.workspaceId, integrationMappings.integrationId, integrationMappings.id] }),
  check("fdp_integration_sync_items_direction_check", sql`${table.direction} IN ('inbound', 'outbound')`),
  check("fdp_integration_sync_items_status_check", sql`${table.status} IN ('pending', 'processed', 'skipped', 'conflict', 'failed')`),
]);

export const integrationJobs = pgTable("fdp_integration_jobs", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  jobType: text("job_type").notNull().default("sync"),
  status: text("status").notNull().default("queued"),
  idempotencyKey: text("idempotency_key").notNull(),
  payloadJson: jsonb("payload_json").$type<Record<string, unknown>>().notNull().default({}),
  attempt: integer("attempt").notNull().default(0),
  maxAttempts: integer("max_attempts").notNull().default(5),
  availableAt: timestamp("available_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  leaseToken: text("lease_token").notNull().default(""),
  leaseExpiresAt: timestamp("lease_expires_at", { withTimezone: true, mode: "string" }),
  lastErrorCode: text("last_error_code").notNull().default(""),
  lastErrorMessage: text("last_error_message").notNull().default(""),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_jobs_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_jobs_idempotency_uq").on(table.workspaceId, table.integrationId, table.idempotencyKey),
  index("fdp_integration_jobs_claim_idx").on(table.workspaceId, table.status, table.availableAt, table.leaseExpiresAt),
  foreignKey({ name: "fdp_integration_jobs_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  check("fdp_integration_jobs_type_check", sql`${table.jobType} IN ('sync', 'retry', 'reconcile', 'health_check')`),
  check("fdp_integration_jobs_status_check", sql`${table.status} IN ('queued', 'leased', 'succeeded', 'failed', 'dead_letter', 'canceled')`),
  check("fdp_integration_jobs_attempt_check", sql`${table.attempt} >= 0 AND ${table.maxAttempts} BETWEEN 1 AND 12 AND ${table.attempt} <= ${table.maxAttempts}`),
]);

export const employeeExternalRefs = pgTable("fdp_employee_external_refs", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  employeeId: text("employee_id").references(() => employees.id, { onDelete: "set null" }),
  source: text("source").notNull().default("sankhya"),
  externalId: text("external_id").notNull(),
  registrationNumber: text("registration_number").notNull().default(""),
  normalizedHash: text("normalized_hash").notNull(),
  normalizedJson: jsonb("normalized_json").$type<Record<string, unknown>>().notNull().default({}),
  lastSeenAt: timestamp("last_seen_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_employee_external_refs_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_employee_external_refs_source_external_uq").on(table.workspaceId, table.integrationId, table.source, table.externalId),
  index("fdp_employee_external_refs_employee_idx").on(table.workspaceId, table.employeeId),
  foreignKey({ name: "fdp_employee_external_refs_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_employee_external_refs_workspace_employee_fk", columns: [table.workspaceId, table.employeeId], foreignColumns: [employees.workspaceId, employees.id] }),
  check("fdp_employee_external_refs_source_check", sql`${table.source} IN ('sankhya')`),
]);

export const employeeSyncChanges = pgTable("fdp_employee_sync_changes", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  externalRefId: text("external_ref_id").notNull().references(() => employeeExternalRefs.id, { onDelete: "cascade" }),
  employeeId: text("employee_id").references(() => employees.id, { onDelete: "set null" }),
  classification: text("classification").notNull(),
  changedFieldsJson: jsonb("changed_fields_json").$type<string[]>().notNull().default([]),
  beforeJson: jsonb("before_json").$type<Record<string, unknown>>().notNull().default({}),
  afterJson: jsonb("after_json").$type<Record<string, unknown>>().notNull().default({}),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_employee_sync_changes_workspace_run_idx").on(table.workspaceId, table.runId, table.createdAt),
  foreignKey({ name: "fdp_employee_sync_changes_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_employee_sync_changes_workspace_ref_fk", columns: [table.workspaceId, table.externalRefId], foreignColumns: [employeeExternalRefs.workspaceId, employeeExternalRefs.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_employee_sync_changes_workspace_employee_fk", columns: [table.workspaceId, table.employeeId], foreignColumns: [employees.workspaceId, employees.id] }),
  check("fdp_employee_sync_changes_classification_check", sql`${table.classification} IN ('new', 'changed', 'unchanged', 'invalid')`),
]);

export const integrationReconciliations = pgTable("fdp_integration_reconciliations", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull().references(() => integrations.id, { onDelete: "cascade" }),
  runId: text("run_id").notNull().references(() => integrationSyncRuns.id, { onDelete: "cascade" }),
  itemId: text("item_id").notNull().references(() => integrationSyncItems.id, { onDelete: "cascade" }),
  entityType: text("entity_type").notNull(),
  externalId: text("external_id").notNull().default(""),
  internalId: text("internal_id").notNull().default(""),
  status: text("status").notNull().default("unmatched"),
  differencesJson: jsonb("differences_json").$type<Record<string, unknown>>().notNull().default({}),
  resolution: text("resolution").notNull().default(""),
  resolutionNote: text("resolution_note").notNull().default(""),
  resolvedBy: text("resolved_by"),
  resolvedAt: timestamp("resolved_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_reconciliations_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_integration_reconciliations_item_uq").on(table.itemId),
  index("fdp_integration_reconciliations_workspace_status_idx").on(table.workspaceId, table.integrationId, table.status, table.createdAt),
  foreignKey({ name: "fdp_integration_reconciliations_workspace_run_fk", columns: [table.workspaceId, table.integrationId, table.runId], foreignColumns: [integrationSyncRuns.workspaceId, integrationSyncRuns.integrationId, integrationSyncRuns.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_reconciliations_workspace_item_fk", columns: [table.workspaceId, table.itemId], foreignColumns: [integrationSyncItems.workspaceId, integrationSyncItems.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_integration_reconciliations_workspace_resolver_fk", columns: [table.workspaceId, table.resolvedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_integration_reconciliations_entity_check", sql`${table.entityType} IN ('inbox', 'employee', 'hr_metric', 'file')`),
  check("fdp_integration_reconciliations_status_check", sql`${table.status} IN ('matched', 'unmatched', 'conflict', 'ignored', 'resolved')`),
  check("fdp_integration_reconciliations_resolution_check", sql`${table.resolution} IN ('', 'link', 'accept_external', 'keep_internal', 'ignore')`),
]);

export const saasPlans = pgTable("fdp_saas_plans", {
  id: text("id").primaryKey(),
  code: text("code").notNull(),
  name: text("name").notNull(),
  description: text("description").notNull().default(""),
  status: text("status").notNull().default("active"),
  currency: text("currency").notNull().default("brl"),
  monthlyPriceCents: integer("monthly_price_cents").notNull().default(0),
  annualPriceCents: integer("annual_price_cents").notNull().default(0),
  trialDays: integer("trial_days").notNull().default(14),
  includedSeats: integer("included_seats").notNull().default(3),
  companyLimit: integer("company_limit").notNull().default(1),
  integrationLimit: integer("integration_limit").notNull().default(1),
  storageLimitMb: integer("storage_limit_mb").notNull().default(1024),
  featuresJson: jsonb("features_json").notNull().default(sql`'[]'::jsonb`),
  stripeMonthlyPriceId: text("stripe_monthly_price_id").notNull().default(""),
  stripeAnnualPriceId: text("stripe_annual_price_id").notNull().default(""),
  position: integer("position").notNull().default(0),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_saas_plans_code_uq").on(table.code),
  index("fdp_saas_plans_status_position_idx").on(table.status, table.position),
  check("fdp_saas_plans_status_check", sql`${table.status} IN ('draft', 'active', 'archived')`),
  check("fdp_saas_plans_currency_check", sql`${table.currency} ~ '^[a-z]{3}$'`),
  check("fdp_saas_plans_prices_check", sql`${table.monthlyPriceCents} >= 0 AND ${table.annualPriceCents} >= 0`),
  check("fdp_saas_plans_limits_check", sql`${table.trialDays} BETWEEN 0 AND 90 AND ${table.includedSeats} > 0 AND ${table.companyLimit} > 0 AND ${table.integrationLimit} >= 0 AND ${table.storageLimitMb} > 0`),
]);

export const workspaceSubscriptions = pgTable("fdp_workspace_subscriptions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  planId: text("plan_id").notNull().references(() => saasPlans.id, { onDelete: "restrict" }),
  status: text("status").notNull().default("trialing"),
  billingInterval: text("billing_interval").notNull().default("monthly"),
  seatQuantity: integer("seat_quantity").notNull().default(1),
  provider: text("provider").notNull().default("stripe"),
  externalCustomerId: text("external_customer_id").notNull().default(""),
  externalSubscriptionId: text("external_subscription_id").notNull().default(""),
  trialEndsAt: timestamp("trial_ends_at", { withTimezone: true, mode: "string" }),
  currentPeriodStartsAt: timestamp("current_period_starts_at", { withTimezone: true, mode: "string" }),
  currentPeriodEndsAt: timestamp("current_period_ends_at", { withTimezone: true, mode: "string" }),
  cancelAtPeriodEnd: integer("cancel_at_period_end").notNull().default(0),
  canceledAt: timestamp("canceled_at", { withTimezone: true, mode: "string" }),
  version: integer("version").notNull().default(1),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_workspace_subscriptions_workspace_uq").on(table.workspaceId),
  uniqueIndex("fdp_workspace_subscriptions_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_workspace_subscriptions_customer_uq").on(table.externalCustomerId).where(sql`${table.externalCustomerId} <> ''`),
  uniqueIndex("fdp_workspace_subscriptions_external_uq").on(table.externalSubscriptionId).where(sql`${table.externalSubscriptionId} <> ''`),
  index("fdp_workspace_subscriptions_status_idx").on(table.status, table.updatedAt),
  check("fdp_workspace_subscriptions_status_check", sql`${table.status} IN ('trialing', 'active', 'past_due', 'paused', 'canceled', 'incomplete')`),
  check("fdp_workspace_subscriptions_interval_check", sql`${table.billingInterval} IN ('monthly', 'annual')`),
  check("fdp_workspace_subscriptions_provider_check", sql`${table.provider} IN ('stripe', 'manual')`),
  check("fdp_workspace_subscriptions_seats_check", sql`${table.seatQuantity} > 0`),
]);

export const workspaceOnboarding = pgTable("fdp_workspace_onboarding", {
  workspaceId: text("workspace_id").primaryKey().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  status: text("status").notNull().default("in_progress"),
  currentStep: text("current_step").notNull().default("company"),
  completedStepsJson: jsonb("completed_steps_json").notNull().default(sql`'[]'::jsonb`),
  skippedStepsJson: jsonb("skipped_steps_json").notNull().default(sql`'[]'::jsonb`),
  profileJson: jsonb("profile_json").notNull().default(sql`'{}'::jsonb`),
  completedAt: timestamp("completed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_workspace_onboarding_status_idx").on(table.status, table.updatedAt),
  check("fdp_workspace_onboarding_status_check", sql`${table.status} IN ('in_progress', 'completed', 'dismissed')`),
  check("fdp_workspace_onboarding_step_check", sql`${table.currentStep} IN ('company', 'team', 'operation', 'integrations', 'billing', 'completed')`),
]);

export const workspaceUsageCounters = pgTable("fdp_workspace_usage_counters", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  metric: text("metric").notNull(),
  period: text("period").notNull(),
  quantity: integer("quantity").notNull().default(0),
  limitSnapshot: integer("limit_snapshot").notNull().default(0),
  measuredAt: timestamp("measured_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_workspace_usage_metric_period_uq").on(table.workspaceId, table.metric, table.period),
  uniqueIndex("fdp_workspace_usage_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_workspace_usage_period_idx").on(table.workspaceId, table.period, table.metric),
  check("fdp_workspace_usage_metric_check", sql`${table.metric} IN ('members', 'companies', 'integrations', 'storage_mb')`),
  check("fdp_workspace_usage_quantity_check", sql`${table.quantity} >= 0 AND ${table.limitSnapshot} >= 0`),
]);

export const billingInvoices = pgTable("fdp_billing_invoices", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  subscriptionId: text("subscription_id").notNull(),
  externalInvoiceId: text("external_invoice_id").notNull(),
  status: text("status").notNull().default("open"),
  currency: text("currency").notNull().default("brl"),
  amountDueCents: integer("amount_due_cents").notNull().default(0),
  amountPaidCents: integer("amount_paid_cents").notNull().default(0),
  hostedInvoiceUrl: text("hosted_invoice_url").notNull().default(""),
  periodStartsAt: timestamp("period_starts_at", { withTimezone: true, mode: "string" }),
  periodEndsAt: timestamp("period_ends_at", { withTimezone: true, mode: "string" }),
  paidAt: timestamp("paid_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_billing_invoices_external_uq").on(table.externalInvoiceId),
  uniqueIndex("fdp_billing_invoices_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_billing_invoices_workspace_created_idx").on(table.workspaceId, table.createdAt),
  foreignKey({ name: "fdp_billing_invoices_workspace_subscription_fk", columns: [table.workspaceId, table.subscriptionId], foreignColumns: [workspaceSubscriptions.workspaceId, workspaceSubscriptions.id] }).onDelete("cascade"),
  check("fdp_billing_invoices_status_check", sql`${table.status} IN ('draft', 'open', 'paid', 'void', 'uncollectible')`),
  check("fdp_billing_invoices_amount_check", sql`${table.amountDueCents} >= 0 AND ${table.amountPaidCents} >= 0`),
]);

export const billingEvents = pgTable("fdp_billing_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  externalEventId: text("external_event_id").notNull(),
  eventType: text("event_type").notNull(),
  status: text("status").notNull().default("processed"),
  objectType: text("object_type").notNull().default(""),
  objectId: text("object_id").notNull().default(""),
  summaryJson: jsonb("summary_json").notNull().default(sql`'{}'::jsonb`),
  errorCode: text("error_code").notNull().default(""),
  receivedAt: timestamp("received_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  processedAt: timestamp("processed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_billing_events_external_uq").on(table.externalEventId),
  uniqueIndex("fdp_billing_events_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_billing_events_workspace_received_idx").on(table.workspaceId, table.receivedAt),
  check("fdp_billing_events_status_check", sql`${table.status} IN ('processed', 'ignored', 'failed')`),
]);

export const platformAuditEvents = pgTable("fdp_platform_audit_events", {
  id: text("id").primaryKey(),
  actorUserId: text("actor_user_id").notNull().references(() => users.id, { onDelete: "restrict" }),
  actorEmail: text("actor_email").notNull(),
  action: text("action").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull().default(""),
  beforeJson: jsonb("before_json").notNull().default(sql`'{}'::jsonb`),
  afterJson: jsonb("after_json").notNull().default(sql`'{}'::jsonb`),
  requestId: text("request_id").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_platform_audit_created_idx").on(table.createdAt),
  index("fdp_platform_audit_created_id_idx").on(table.createdAt, table.id),
  index("fdp_platform_audit_actor_idx").on(table.actorUserId, table.createdAt),
  index("fdp_platform_audit_entity_created_idx").on(table.entityType, table.entityId, table.createdAt),
]);

export const hrMetrics = pgTable("fdp_hr_metrics", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  period: text("period").notNull(),
  headcount: integer("headcount").notNull().default(0),
  headcountStart: integer("headcount_start").notNull().default(0),
  headcountEnd: integer("headcount_end").notNull().default(0),
  leavesCount: integer("leaves_count").notNull().default(0),
  admissions: integer("admissions").notNull().default(0),
  terminations: integer("terminations").notNull().default(0),
  voluntaryTerminations: integer("voluntary_terminations").notNull().default(0),
  involuntaryTerminations: integer("involuntary_terminations").notNull().default(0),
  baseSalary: numeric("base_salary", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  variablePay: numeric("variable_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  overtimePay: numeric("overtime_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  additionalPay: numeric("additional_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  vacationPay: numeric("vacation_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  thirteenthPay: numeric("thirteenth_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  terminationPay: numeric("termination_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  grossPayroll: numeric("gross_payroll", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  employeeInss: numeric("employee_inss", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  employeeIrrf: numeric("employee_irrf", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  employeeOtherDeductions: numeric("employee_other_deductions", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  netPay: numeric("net_pay", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  employerInss: numeric("employer_inss", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  ratContribution: numeric("rat_contribution", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  thirdPartyContributions: numeric("third_party_contributions", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  fgts: numeric("fgts", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  fgtsPenalty: numeric("fgts_penalty", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  employerCharges: numeric("employer_charges", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  benefitsCost: numeric("benefits_cost", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  provisionsCost: numeric("provisions_cost", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  otherCosts: numeric("other_costs", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  payrollCost: numeric("payroll_cost", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  source: text("source").notNull().default("manual"),
  externalId: text("external_id").notNull().default(""),
  notes: text("notes").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_hr_metrics_workspace_company_period_uq").on(table.workspaceId, table.companyId, table.period),
  index("fdp_hr_metrics_workspace_period_idx").on(table.workspaceId, table.period),
  foreignKey({
    name: "fdp_hr_metrics_workspace_company_fk",
    columns: [table.workspaceId, table.companyId],
    foreignColumns: [companies.workspaceId, companies.id],
  }).onDelete("cascade"),
]);

/* -------------------------------------------------------------------------- */
/* Controle de pagamento — psicólogos e prestadores PJ                         */
/* -------------------------------------------------------------------------- */

export const psychologistProfiles = pgTable("fdp_psychologist_profiles", {
  providerId: text("provider_id").primaryKey().references(() => auxiliaryProviders.id, { onDelete: "cascade" }),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  defaultSessionAmount: numeric("default_session_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  payoutEncrypted: text("payout_encrypted").notNull().default(""),
  payoutIv: text("payout_iv").notNull().default(""),
  payoutTag: text("payout_tag").notNull().default(""),
  payoutKeyVersion: integer("payout_key_version").notNull().default(0),
  administrativeNotes: text("administrative_notes").notNull().default(""),
  status: text("status").notNull().default("active"),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_psychologist_profiles_workspace_provider_uq").on(table.workspaceId, table.providerId),
  foreignKey({ name: "fdp_psychologist_profiles_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }).onDelete("cascade"),
  check("fdp_psychologist_profiles_amount_check", sql`${table.defaultSessionAmount} >= 0`),
  check("fdp_psychologist_profiles_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const psychologyClosings = pgTable("fdp_psychology_closings", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  providerId: text("provider_id").notNull().references(() => auxiliaryProviders.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "restrict" }),
  competence: text("competence").notNull(),
  entriesCount: integer("entries_count").notNull().default(0),
  sessionsCount: integer("sessions_count").notNull().default(0),
  employeesCount: integer("employees_count").notNull().default(0),
  grossAmount: numeric("gross_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  adjustmentsAmount: numeric("adjustments_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  netAmount: numeric("net_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  status: text("status").notNull().default("open"),
  calcVersion: text("calc_version").notNull(),
  snapshotJson: jsonb("snapshot_json").$type<Record<string, unknown>>().notNull().default({}),
  approvedBy: text("approved_by"),
  approvedAt: timestamp("approved_at", { withTimezone: true, mode: "string" }),
  closedBy: text("closed_by"),
  closedAt: timestamp("closed_at", { withTimezone: true, mode: "string" }),
  reopenReason: text("reopen_reason").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_psychology_closings_workspace_provider_cycle_uq").on(table.workspaceId, table.providerId, table.payrollCycleId),
  uniqueIndex("fdp_psychology_closings_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_psychology_closings_workspace_competence_idx").on(table.workspaceId, table.competence, table.status),
  foreignKey({ name: "fdp_psychology_closings_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_psychology_closings_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  foreignKey({ name: "fdp_psychology_closings_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_psychology_closings_workspace_approver_fk", columns: [table.workspaceId, table.approvedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_psychology_closings_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_psychology_closings_status_check", sql`${table.status} IN ('open', 'review', 'approval', 'scheduled', 'paid', 'closed', 'reopened')`),
  check("fdp_psychology_closings_amount_check", sql`${table.grossAmount} >= 0 AND ${table.netAmount} >= 0`),
  check("fdp_psychology_closings_count_check", sql`${table.entriesCount} >= 0 AND ${table.sessionsCount} >= 0 AND ${table.employeesCount} >= 0`),
]);

export const psychologySessions = pgTable("fdp_psychology_sessions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  providerId: text("provider_id").notNull().references(() => auxiliaryProviders.id, { onDelete: "restrict" }),
  employeeId: text("employee_id").notNull().references(() => employees.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "restrict" }),
  closingId: text("closing_id"),
  competence: text("competence").notNull(),
  sessionDate: date("session_date", { mode: "string" }).notNull(),
  sessionQuantity: integer("session_quantity").notNull().default(1),
  unitAmount: numeric("unit_amount", { precision: 18, scale: 2, mode: "number" }).notNull(),
  totalAmount: numeric("total_amount", { precision: 18, scale: 2, mode: "number" }).notNull(),
  origin: text("origin").notNull().default("manual"),
  status: text("status").notNull().default("registered"),
  administrativeNote: text("administrative_note").notNull().default(""),
  externalId: text("external_id").notNull().default(""),
  canceledReason: text("canceled_reason").notNull().default(""),
  canceledBy: text("canceled_by"),
  canceledAt: timestamp("canceled_at", { withTimezone: true, mode: "string" }),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_psychology_sessions_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_psychology_sessions_workspace_external_uq").on(table.workspaceId, table.externalId).where(sql`${table.externalId} <> ''`),
  index("fdp_psychology_sessions_workspace_cycle_provider_idx").on(table.workspaceId, table.payrollCycleId, table.providerId, table.status),
  index("fdp_psychology_sessions_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.competence),
  foreignKey({ name: "fdp_psychology_sessions_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_psychology_sessions_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  foreignKey({ name: "fdp_psychology_sessions_workspace_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_psychology_sessions_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_psychology_sessions_workspace_closing_fk", columns: [table.workspaceId, table.closingId], foreignColumns: [psychologyClosings.workspaceId, psychologyClosings.id] }),
  check("fdp_psychology_sessions_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_psychology_sessions_quantity_check", sql`${table.sessionQuantity} > 0 AND ${table.sessionQuantity} <= 10000`),
  check("fdp_psychology_sessions_amount_check", sql`${table.unitAmount} >= 0 AND ${table.totalAmount} >= 0`),
  check("fdp_psychology_sessions_total_check", sql`${table.totalAmount} = ${table.unitAmount} * ${table.sessionQuantity}`),
  check("fdp_psychology_sessions_origin_check", sql`${table.origin} IN ('manual', 'import', 'integration')`),
  check("fdp_psychology_sessions_status_check", sql`${table.status} IN ('registered', 'canceled')`),
]);

export const psychologyAdjustments = pgTable("fdp_psychology_adjustments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  closingId: text("closing_id").notNull().references(() => psychologyClosings.id, { onDelete: "cascade" }),
  kind: text("kind").notNull(),
  amount: numeric("amount", { precision: 18, scale: 2, mode: "number" }).notNull(),
  reason: text("reason").notNull(),
  previousAmount: numeric("previous_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  newAmount: numeric("new_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_psychology_adjustments_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_psychology_adjustments_workspace_closing_idx").on(table.workspaceId, table.closingId, table.createdAt),
  foreignKey({ name: "fdp_psychology_adjustments_workspace_closing_fk", columns: [table.workspaceId, table.closingId], foreignColumns: [psychologyClosings.workspaceId, psychologyClosings.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_psychology_adjustments_workspace_author_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_psychology_adjustments_kind_check", sql`${table.kind} IN ('inclusion', 'cancellation', 'correction', 'discount', 'complement')`),
  check("fdp_psychology_adjustments_amount_check", sql`${table.amount} >= 0`),
  check("fdp_psychology_adjustments_reason_check", sql`length(${table.reason}) >= 5`),
]);

export const psychologyPayments = pgTable("fdp_psychology_payments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  closingId: text("closing_id").notNull().references(() => psychologyClosings.id, { onDelete: "restrict" }),
  amount: numeric("amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  method: text("method").notNull().default("pix"),
  status: text("status").notNull().default("pending"),
  scheduledDate: date("scheduled_date", { mode: "string" }),
  paidDate: date("paid_date", { mode: "string" }),
  invoiceNumber: text("invoice_number").notNull().default(""),
  invoiceAmount: numeric("invoice_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  invoiceIssueDate: date("invoice_issue_date", { mode: "string" }),
  invoiceStatus: text("invoice_status").notNull().default("not_required"),
  receiptReference: text("receipt_reference").notNull().default(""),
  responsibleUserId: text("responsible_user_id"),
  note: text("note").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_psychology_payments_workspace_closing_uq").on(table.workspaceId, table.closingId),
  uniqueIndex("fdp_psychology_payments_workspace_id_uq").on(table.workspaceId, table.id),
  foreignKey({ name: "fdp_psychology_payments_workspace_closing_fk", columns: [table.workspaceId, table.closingId], foreignColumns: [psychologyClosings.workspaceId, psychologyClosings.id] }).onDelete("restrict"),
  foreignKey({ name: "fdp_psychology_payments_workspace_responsible_fk", columns: [table.workspaceId, table.responsibleUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_psychology_payments_method_check", sql`${table.method} IN ('pix', 'bank_transfer', 'invoice_payment', 'other')`),
  check("fdp_psychology_payments_status_check", sql`${table.status} IN ('pending', 'scheduled', 'paid', 'canceled')`),
  check("fdp_psychology_payments_invoice_status_check", sql`${table.invoiceStatus} IN ('not_required', 'pending', 'received', 'validated', 'divergent')`),
  check("fdp_psychology_payments_amount_check", sql`${table.amount} >= 0 AND ${table.invoiceAmount} >= 0`),
]);

export const contractorProfiles = pgTable("fdp_contractor_profiles", {
  providerId: text("provider_id").primaryKey().references(() => auxiliaryProviders.id, { onDelete: "cascade" }),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  contractReference: text("contract_reference").notNull().default(""),
  roleTitle: text("role_title").notNull().default(""),
  departmentId: text("department_id"),
  costCenterId: text("cost_center_id"),
  responsibleUserId: text("responsible_user_id"),
  contractStart: date("contract_start", { mode: "string" }),
  contractEnd: date("contract_end", { mode: "string" }),
  baseAmount: numeric("base_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  invoiceLimitOverride: numeric("invoice_limit_override", { precision: 18, scale: 2, mode: "number" }),
  complementMethod: text("complement_method").notNull().default("none"),
  complementPlatform: text("complement_platform").notNull().default(""),
  complementExternalId: text("complement_external_id").notNull().default(""),
  payoutEncrypted: text("payout_encrypted").notNull().default(""),
  payoutIv: text("payout_iv").notNull().default(""),
  payoutTag: text("payout_tag").notNull().default(""),
  payoutKeyVersion: integer("payout_key_version").notNull().default(0),
  status: text("status").notNull().default("active"),
  notes: text("notes").notNull().default(""),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_contractor_profiles_workspace_provider_uq").on(table.workspaceId, table.providerId),
  index("fdp_contractor_profiles_workspace_company_idx").on(table.workspaceId, table.companyId, table.status),
  foreignKey({ name: "fdp_contractor_profiles_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_contractor_profiles_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_contractor_profiles_workspace_department_fk", columns: [table.workspaceId, table.companyId, table.departmentId], foreignColumns: [departments.workspaceId, departments.companyId, departments.id] }),
  foreignKey({ name: "fdp_contractor_profiles_workspace_cost_center_fk", columns: [table.workspaceId, table.companyId, table.costCenterId], foreignColumns: [costCenters.workspaceId, costCenters.companyId, costCenters.id] }),
  foreignKey({ name: "fdp_contractor_profiles_workspace_responsible_fk", columns: [table.workspaceId, table.responsibleUserId], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_contractor_profiles_base_check", sql`${table.baseAmount} >= 0`),
  check("fdp_contractor_profiles_limit_check", sql`${table.invoiceLimitOverride} IS NULL OR ${table.invoiceLimitOverride} >= 0`),
  check("fdp_contractor_profiles_complement_check", sql`${table.complementMethod} IN ('none', 'caju_saldo_livre', 'other_benefit_card', 'manual_transfer')`),
  check("fdp_contractor_profiles_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const invoiceLimitPolicies = pgTable("fdp_invoice_limit_policies", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  scope: text("scope").notNull(),
  companyId: text("company_id"),
  providerId: text("provider_id"),
  contractReference: text("contract_reference").notNull().default(""),
  amount: numeric("amount", { precision: 18, scale: 2, mode: "number" }).notNull(),
  effectiveFrom: date("effective_from", { mode: "string" }).notNull(),
  effectiveTo: date("effective_to", { mode: "string" }),
  note: text("note").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_invoice_limit_policies_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_invoice_limit_policies_workspace_scope_idx").on(table.workspaceId, table.scope, table.effectiveFrom),
  foreignKey({ name: "fdp_invoice_limit_policies_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_invoice_limit_policies_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  check("fdp_invoice_limit_policies_scope_check", sql`${table.scope} IN ('workspace', 'company', 'contract', 'provider')`),
  check("fdp_invoice_limit_policies_amount_check", sql`${table.amount} >= 0`),
  check("fdp_invoice_limit_policies_target_check", sql`(${table.scope} = 'workspace' AND ${table.companyId} IS NULL AND ${table.providerId} IS NULL)
    OR (${table.scope} = 'company' AND ${table.companyId} IS NOT NULL AND ${table.providerId} IS NULL)
    OR (${table.scope} = 'contract' AND ${table.companyId} IS NOT NULL AND ${table.contractReference} <> '')
    OR (${table.scope} = 'provider' AND ${table.providerId} IS NOT NULL)`),
]);

export const contractorClosings = pgTable("fdp_contractor_closings", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  providerId: text("provider_id").notNull().references(() => auxiliaryProviders.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "restrict" }),
  competence: text("competence").notNull(),
  baseAmount: numeric("base_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  creditsAmount: numeric("credits_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  debitsAmount: numeric("debits_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  netAmount: numeric("net_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  invoiceLimitAmount: numeric("invoice_limit_amount", { precision: 18, scale: 2, mode: "number" }),
  invoiceLimitSource: text("invoice_limit_source").notNull().default("none"),
  invoiceLimitPolicyId: text("invoice_limit_policy_id"),
  invoiceExpectedAmount: numeric("invoice_expected_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  complementAmount: numeric("complement_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  complementMethod: text("complement_method").notNull().default("none"),
  cajuAmount: numeric("caju_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  status: text("status").notNull().default("open"),
  invoiceNumber: text("invoice_number").notNull().default(""),
  invoiceReceivedAmount: numeric("invoice_received_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  invoiceIssueDate: date("invoice_issue_date", { mode: "string" }),
  invoiceStatus: text("invoice_status").notNull().default("pending"),
  invoiceAttachmentReference: text("invoice_attachment_reference").notNull().default(""),
  cajuStatus: text("caju_status").notNull().default("not_required"),
  cajuBatchReference: text("caju_batch_reference").notNull().default(""),
  cajuExternalId: text("caju_external_id").notNull().default(""),
  cajuError: text("caju_error").notNull().default(""),
  complementPaidAmount: numeric("complement_paid_amount", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  reconciliationStatus: text("reconciliation_status").notNull().default("pending"),
  reconciliationDifference: numeric("reconciliation_difference", { precision: 18, scale: 2, mode: "number" }).notNull().default(0),
  calcVersion: text("calc_version").notNull(),
  snapshotJson: jsonb("snapshot_json").$type<Record<string, unknown>>().notNull().default({}),
  approvedBy: text("approved_by"),
  approvedAt: timestamp("approved_at", { withTimezone: true, mode: "string" }),
  closedBy: text("closed_by"),
  closedAt: timestamp("closed_at", { withTimezone: true, mode: "string" }),
  reopenReason: text("reopen_reason").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_contractor_closings_workspace_provider_cycle_uq").on(table.workspaceId, table.providerId, table.payrollCycleId),
  uniqueIndex("fdp_contractor_closings_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_contractor_closings_workspace_competence_idx").on(table.workspaceId, table.competence, table.status),
  foreignKey({ name: "fdp_contractor_closings_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_contractor_closings_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  foreignKey({ name: "fdp_contractor_closings_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_contractor_closings_workspace_limit_policy_fk", columns: [table.workspaceId, table.invoiceLimitPolicyId], foreignColumns: [invoiceLimitPolicies.workspaceId, invoiceLimitPolicies.id] }),
  foreignKey({ name: "fdp_contractor_closings_workspace_approver_fk", columns: [table.workspaceId, table.approvedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_contractor_closings_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_contractor_closings_status_check", sql`${table.status} IN ('open', 'review', 'approval', 'approved', 'invoice_pending', 'ready_to_pay', 'paid', 'closed', 'reopened')`),
  check("fdp_contractor_closings_invoice_status_check", sql`${table.invoiceStatus} IN ('not_required', 'pending', 'received', 'validated', 'divergent')`),
  check("fdp_contractor_closings_caju_status_check", sql`${table.cajuStatus} IN ('not_required', 'pending', 'approved', 'sent', 'processed', 'error', 'canceled')`),
  check("fdp_contractor_closings_reconciliation_check", sql`${table.reconciliationStatus} IN ('pending', 'reconciled', 'divergent')`),
  check("fdp_contractor_closings_limit_source_check", sql`${table.invoiceLimitSource} IN ('none', 'workspace', 'company', 'contract', 'provider')`),
  check("fdp_contractor_closings_complement_check", sql`${table.complementMethod} IN ('none', 'caju_saldo_livre', 'other_benefit_card', 'manual_transfer')`),
  check("fdp_contractor_closings_amount_check", sql`${table.baseAmount} >= 0 AND ${table.creditsAmount} >= 0 AND ${table.debitsAmount} >= 0 AND ${table.netAmount} >= 0
    AND ${table.invoiceExpectedAmount} >= 0 AND ${table.complementAmount} >= 0 AND ${table.cajuAmount} >= 0 AND ${table.invoiceReceivedAmount} >= 0 AND ${table.complementPaidAmount} >= 0`),
  check("fdp_contractor_closings_split_check", sql`${table.invoiceExpectedAmount} + ${table.complementAmount} = ${table.netAmount}`),
  check("fdp_contractor_closings_limit_cap_check", sql`${table.invoiceLimitAmount} IS NULL OR ${table.invoiceExpectedAmount} <= ${table.invoiceLimitAmount}`),
]);

export const contractorComponents = pgTable("fdp_contractor_components", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  providerId: text("provider_id").notNull().references(() => auxiliaryProviders.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull().references(() => payrollCycles.id, { onDelete: "restrict" }),
  closingId: text("closing_id"),
  competence: text("competence").notNull(),
  direction: text("direction").notNull(),
  componentType: text("component_type").notNull(),
  description: text("description").notNull().default(""),
  componentQuantity: numeric("component_quantity", { precision: 18, scale: 4, mode: "number" }).notNull().default(1),
  amount: numeric("amount", { precision: 18, scale: 2, mode: "number" }).notNull(),
  origin: text("origin").notNull().default("manual"),
  documentReference: text("document_reference").notNull().default(""),
  note: text("note").notNull().default(""),
  status: text("status").notNull().default("active"),
  externalId: text("external_id").notNull().default(""),
  canceledReason: text("canceled_reason").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_contractor_components_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_contractor_components_workspace_external_uq").on(table.workspaceId, table.externalId).where(sql`${table.externalId} <> ''`),
  index("fdp_contractor_components_workspace_cycle_provider_idx").on(table.workspaceId, table.payrollCycleId, table.providerId, table.status),
  foreignKey({ name: "fdp_contractor_components_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  foreignKey({ name: "fdp_contractor_components_workspace_provider_fk", columns: [table.workspaceId, table.providerId], foreignColumns: [auxiliaryProviders.workspaceId, auxiliaryProviders.id] }),
  foreignKey({ name: "fdp_contractor_components_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_contractor_components_workspace_closing_fk", columns: [table.workspaceId, table.closingId], foreignColumns: [contractorClosings.workspaceId, contractorClosings.id] }),
  check("fdp_contractor_components_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_contractor_components_direction_check", sql`${table.direction} IN ('credit', 'debit')`),
  check("fdp_contractor_components_status_check", sql`${table.status} IN ('active', 'canceled')`),
  check("fdp_contractor_components_origin_check", sql`${table.origin} IN ('manual', 'import', 'integration')`),
  check("fdp_contractor_components_amount_check", sql`${table.amount} >= 0`),
  check("fdp_contractor_components_type_check", sql`(${table.direction} = 'credit' AND ${table.componentType} IN ('base', 'commission', 'bonus', 'award', 'reimbursement', 'additional', 'positive_adjustment', 'other_credit'))
    OR (${table.direction} = 'debit' AND ${table.componentType} IN ('health_plan', 'dental_plan', 'benefit', 'coparticipation', 'equipment', 'advance', 'absence', 'loan', 'negative_adjustment', 'other_debit'))`),
]);

/* -------------------------------------------------------------------------- */
/* Escala — outbox transacional, webhooks de saída e API pública               */
/* -------------------------------------------------------------------------- */

export const domainEvents = pgTable("fdp_domain_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  eventType: text("event_type").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull().default(""),
  payloadJson: jsonb("payload_json").$type<Record<string, unknown>>().notNull().default({}),
  status: text("status").notNull().default("pending"),
  actorUserId: text("actor_user_id"),
  requestId: text("request_id").notNull().default(""),
  occurredAt: timestamp("occurred_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  publishedAt: timestamp("published_at", { withTimezone: true, mode: "string" }),
  deliveriesCount: integer("deliveries_count").notNull().default(0),
}, (table) => [
  uniqueIndex("fdp_domain_events_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_domain_events_pending_idx").on(table.workspaceId, table.status, table.occurredAt),
  index("fdp_domain_events_type_idx").on(table.workspaceId, table.eventType, table.occurredAt),
  check("fdp_domain_events_status_check", sql`${table.status} IN ('pending', 'published', 'skipped')`),
]);

export const webhookEndpoints = pgTable("fdp_webhook_endpoints", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  url: text("url").notNull(),
  secretEncrypted: text("secret_encrypted").notNull(),
  secretIv: text("secret_iv").notNull(),
  secretTag: text("secret_tag").notNull(),
  secretKeyVersion: integer("secret_key_version").notNull().default(1),
  eventTypesJson: jsonb("event_types_json").$type<string[]>().notNull().default([]),
  status: text("status").notNull().default("active"),
  failureCount: integer("failure_count").notNull().default(0),
  lastDeliveryAt: timestamp("last_delivery_at", { withTimezone: true, mode: "string" }),
  lastFailureAt: timestamp("last_failure_at", { withTimezone: true, mode: "string" }),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_webhook_endpoints_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_webhook_endpoints_workspace_url_uq").on(table.workspaceId, table.url),
  index("fdp_webhook_endpoints_status_idx").on(table.workspaceId, table.status),
  check("fdp_webhook_endpoints_status_check", sql`${table.status} IN ('active', 'paused', 'disabled')`),
  check("fdp_webhook_endpoints_url_check", sql`${table.url} LIKE 'https://%'`),
]);

export const webhookDeliveries = pgTable("fdp_webhook_deliveries", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  endpointId: text("endpoint_id").notNull().references(() => webhookEndpoints.id, { onDelete: "cascade" }),
  eventId: text("event_id").notNull().references(() => domainEvents.id, { onDelete: "cascade" }),
  eventType: text("event_type").notNull(),
  status: text("status").notNull().default("pending"),
  attempt: integer("attempt").notNull().default(0),
  maxAttempts: integer("max_attempts").notNull().default(6),
  nextAttemptAt: timestamp("next_attempt_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  leaseToken: text("lease_token").notNull().default(""),
  leaseExpiresAt: timestamp("lease_expires_at", { withTimezone: true, mode: "string" }),
  responseStatus: integer("response_status").notNull().default(0),
  responseSnippet: text("response_snippet").notNull().default(""),
  errorCode: text("error_code").notNull().default(""),
  deliveredAt: timestamp("delivered_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_webhook_deliveries_endpoint_event_uq").on(table.workspaceId, table.endpointId, table.eventId),
  uniqueIndex("fdp_webhook_deliveries_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_webhook_deliveries_queue_idx").on(table.workspaceId, table.status, table.nextAttemptAt),
  foreignKey({ name: "fdp_webhook_deliveries_workspace_endpoint_fk", columns: [table.workspaceId, table.endpointId], foreignColumns: [webhookEndpoints.workspaceId, webhookEndpoints.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_webhook_deliveries_workspace_event_fk", columns: [table.workspaceId, table.eventId], foreignColumns: [domainEvents.workspaceId, domainEvents.id] }).onDelete("cascade"),
  check("fdp_webhook_deliveries_status_check", sql`${table.status} IN ('pending', 'delivering', 'delivered', 'failed', 'dead_letter', 'canceled')`),
  check("fdp_webhook_deliveries_attempt_check", sql`${table.attempt} >= 0 AND ${table.maxAttempts} > 0`),
]);

export const apiKeys = pgTable("fdp_api_keys", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  name: text("name").notNull(),
  prefix: text("prefix").notNull(),
  tokenHash: text("token_hash").notNull(),
  scopesJson: jsonb("scopes_json").$type<string[]>().notNull().default([]),
  rateLimitPerMinute: integer("rate_limit_per_minute").notNull().default(120),
  status: text("status").notNull().default("active"),
  lastUsedAt: timestamp("last_used_at", { withTimezone: true, mode: "string" }),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }),
  revokedAt: timestamp("revoked_at", { withTimezone: true, mode: "string" }),
  revokedBy: text("revoked_by"),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_api_keys_prefix_uq").on(table.prefix),
  uniqueIndex("fdp_api_keys_token_hash_uq").on(table.tokenHash),
  uniqueIndex("fdp_api_keys_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_api_keys_workspace_status_idx").on(table.workspaceId, table.status),
  check("fdp_api_keys_status_check", sql`${table.status} IN ('active', 'revoked')`),
  check("fdp_api_keys_rate_check", sql`${table.rateLimitPerMinute} > 0 AND ${table.rateLimitPerMinute} <= 6000`),
]);

export const apiRateLimits = pgTable("fdp_api_rate_limits", {
  keyHash: text("key_hash").primaryKey(),
  windowStartedAt: timestamp("window_started_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  requestCount: integer("request_count").notNull().default(0),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_api_rate_limits_window_idx").on(table.windowStartedAt),
  check("fdp_api_rate_limits_count_check", sql`${table.requestCount} >= 0`),
]);

export const apiIdempotencyRecords = pgTable("fdp_api_idempotency_records", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  apiKeyId: text("api_key_id").notNull(),
  idempotencyKey: text("idempotency_key").notNull(),
  method: text("method").notNull(),
  path: text("path").notNull(),
  requestHash: text("request_hash").notNull(),
  responseStatus: integer("response_status").notNull().default(0),
  responseBody: text("response_body").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  expiresAt: timestamp("expires_at", { withTimezone: true, mode: "string" }).notNull(),
}, (table) => [
  uniqueIndex("fdp_api_idempotency_key_uq").on(table.workspaceId, table.apiKeyId, table.idempotencyKey),
  uniqueIndex("fdp_api_idempotency_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_api_idempotency_expires_idx").on(table.expiresAt),
  foreignKey({ name: "fdp_api_idempotency_workspace_key_fk", columns: [table.workspaceId, table.apiKeyId], foreignColumns: [apiKeys.workspaceId, apiKeys.id] }).onDelete("cascade"),
]);

/* -------------------------------------------------------------------------- */
/* Site comercial — contatos recebidos antes de existir workspace              */
/* -------------------------------------------------------------------------- */

export const marketingLeads = pgTable("fdp_marketing_leads", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  email: text("email").notNull(),
  company: text("company").notNull().default(""),
  phone: text("phone").notNull().default(""),
  headcount: text("headcount").notNull().default(""),
  interest: text("interest").notNull(),
  message: text("message").notNull().default(""),
  consentAt: timestamp("consent_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  sourcePath: text("source_path").notNull().default(""),
  status: text("status").notNull().default("new"),
  handledBy: text("handled_by"),
  handledAt: timestamp("handled_at", { withTimezone: true, mode: "string" }),
  requestId: text("request_id").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  index("fdp_marketing_leads_created_idx").on(table.createdAt),
  index("fdp_marketing_leads_status_idx").on(table.status, table.createdAt),
  check("fdp_marketing_leads_status_check", sql`${table.status} IN ('new', 'contacted', 'qualified', 'discarded')`),
  check("fdp_marketing_leads_interest_check", sql`${table.interest} IN ('demonstracao', 'planos', 'integracoes', 'suporte', 'outro')`),
]);

/* -------------------------------------------------------------------------- */
/* Conferência de ponto (§28) e destino dos eventos de hora (§22)              */
/* -------------------------------------------------------------------------- */

export const hourEventMappings = pgTable("fdp_hour_event_mappings", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "cascade" }),
  destination: text("destination").notNull(),
  eventCode: text("event_code").notNull(),
  rubricCode: text("rubric_code").notNull(),
  /** §22: evento de hora vai para quantidade, referência ou índice — nunca para valor. */
  targetField: text("target_field").notNull(),
  unit: text("unit").notNull(),
  status: text("status").notNull().default("active"),
  note: text("note").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_hour_event_mappings_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_hour_event_mappings_scope_event_uq").on(table.workspaceId, table.companyId, table.destination, table.eventCode),
  index("fdp_hour_event_mappings_workspace_status_idx").on(table.workspaceId, table.companyId, table.status),
  foreignKey({ name: "fdp_hour_event_mappings_workspace_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_hour_event_mappings_workspace_author_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_hour_event_mappings_event_check", sql`${table.eventCode} IN ('overtime_50', 'overtime_100', 'night_hours', 'late_hours', 'absence_hours', 'interval_not_taken', 'on_call_hours', 'time_bank_credit', 'time_bank_debit')`),
  check("fdp_hour_event_mappings_target_check", sql`${table.targetField} IN ('quantity', 'reference', 'index')`),
  check("fdp_hour_event_mappings_unit_check", sql`${table.unit} IN ('hours_decimal', 'hours_minutes', 'minutes')`),
  check("fdp_hour_event_mappings_status_check", sql`${table.status} IN ('active', 'inactive')`),
  check("fdp_hour_event_mappings_rubric_check", sql`length(${table.rubricCode}) > 0`),
  check("fdp_hour_event_mappings_destination_check", sql`length(${table.destination}) > 0`),
]);

export const timeExports = pgTable("fdp_time_exports", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull(),
  competence: text("competence").notNull(),
  destination: text("destination").notNull(),
  status: text("status").notNull().default("prepared"),
  sheetsCount: integer("sheets_count").notNull().default(0),
  linesCount: integer("lines_count").notNull().default(0),
  checksum: text("checksum").notNull(),
  payloadJson: jsonb("payload_json").$type<unknown[]>().notNull().default([]),
  error: text("error").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_time_exports_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_time_exports_workspace_cycle_idx").on(table.workspaceId, table.payrollCycleId, table.createdAt),
  foreignKey({ name: "fdp_time_exports_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_time_exports_workspace_author_fk", columns: [table.workspaceId, table.createdBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_time_exports_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_time_exports_status_check", sql`${table.status} IN ('prepared', 'delivered', 'failed')`),
  check("fdp_time_exports_count_check", sql`${table.sheetsCount} >= 0 AND ${table.linesCount} > 0`),
]);

export const timeSheets = pgTable("fdp_time_sheets", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull().references(() => companies.id, { onDelete: "restrict" }),
  employeeId: text("employee_id").notNull().references(() => employees.id, { onDelete: "restrict" }),
  payrollCycleId: text("payroll_cycle_id").notNull(),
  competence: text("competence").notNull(),
  status: text("status").notNull().default("draft"),
  source: text("source").notNull().default("manual"),
  entriesCount: integer("entries_count").notNull().default(0),
  workedMinutes: integer("worked_minutes").notNull().default(0),
  expectedMinutes: integer("expected_minutes").notNull().default(0),
  balanceMinutes: integer("balance_minutes").notNull().default(0),
  blockingIssues: integer("blocking_issues").notNull().default(0),
  warningIssues: integer("warning_issues").notNull().default(0),
  calcVersion: text("calc_version").notNull(),
  snapshotJson: jsonb("snapshot_json").$type<Record<string, unknown>>().notNull().default({}),
  approvedBy: text("approved_by"),
  approvedAt: timestamp("approved_at", { withTimezone: true, mode: "string" }),
  rejectedReason: text("rejected_reason").notNull().default(""),
  exportedAt: timestamp("exported_at", { withTimezone: true, mode: "string" }),
  exportId: text("export_id"),
  reopenReason: text("reopen_reason").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_time_sheets_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_time_sheets_workspace_employee_cycle_uq").on(table.workspaceId, table.employeeId, table.payrollCycleId),
  index("fdp_time_sheets_workspace_status_idx").on(table.workspaceId, table.competence, table.status),
  index("fdp_time_sheets_workspace_company_cycle_idx").on(table.workspaceId, table.companyId, table.payrollCycleId, table.status),
  foreignKey({ name: "fdp_time_sheets_workspace_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_time_sheets_workspace_cycle_fk", columns: [table.workspaceId, table.companyId, table.payrollCycleId], foreignColumns: [payrollCycles.workspaceId, payrollCycles.companyId, payrollCycles.id] }),
  foreignKey({ name: "fdp_time_sheets_workspace_approver_fk", columns: [table.workspaceId, table.approvedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  foreignKey({ name: "fdp_time_sheets_workspace_export_fk", columns: [table.workspaceId, table.exportId], foreignColumns: [timeExports.workspaceId, timeExports.id] }),
  check("fdp_time_sheets_competence_check", sql`${table.competence} ~ '^[0-9]{4}-(0[1-9]|1[0-2])$'`),
  check("fdp_time_sheets_status_check", sql`${table.status} IN ('draft', 'review', 'approved', 'rejected', 'exported', 'closed')`),
  check("fdp_time_sheets_source_check", sql`${table.source} IN ('manual', 'import', 'integration')`),
  check("fdp_time_sheets_count_check", sql`${table.entriesCount} >= 0 AND ${table.blockingIssues} >= 0 AND ${table.warningIssues} >= 0`),
  check("fdp_time_sheets_minutes_check", sql`${table.workedMinutes} >= 0 AND ${table.expectedMinutes} >= 0`),
  check("fdp_time_sheets_balance_check", sql`${table.balanceMinutes} = ${table.workedMinutes} - ${table.expectedMinutes}`),
  check("fdp_time_sheets_approved_check", sql`${table.status} NOT IN ('approved', 'exported', 'closed') OR ${table.approvedBy} IS NOT NULL`),
  check("fdp_time_sheets_export_check", sql`${table.status} <> 'exported' OR ${table.exportId} IS NOT NULL`),
]);

export const timeEntries = pgTable("fdp_time_entries", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  timeSheetId: text("time_sheet_id").notNull().references(() => timeSheets.id, { onDelete: "cascade" }),
  employeeId: text("employee_id").notNull(),
  entryDate: date("entry_date", { mode: "string" }).notNull(),
  dayType: text("day_type").notNull().default("regular"),
  expectedMinutes: integer("expected_minutes").notNull().default(0),
  workedMinutes: integer("worked_minutes").notNull().default(0),
  nightMinutes: integer("night_minutes").notNull().default(0),
  balanceMinutes: integer("balance_minutes").notNull().default(0),
  /** Somente pares `{ in, out }`: qualquer outro campo do relógio é descartado antes de gravar. */
  punchesJson: jsonb("punches_json").$type<{ in: string; out: string }[]>().notNull().default([]),
  origin: text("origin").notNull().default("manual"),
  justification: text("justification").notNull().default(""),
  note: text("note").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_time_entries_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_time_entries_sheet_date_uq").on(table.workspaceId, table.timeSheetId, table.entryDate),
  index("fdp_time_entries_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.entryDate),
  foreignKey({ name: "fdp_time_entries_workspace_sheet_fk", columns: [table.workspaceId, table.timeSheetId], foreignColumns: [timeSheets.workspaceId, timeSheets.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_time_entries_workspace_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  check("fdp_time_entries_day_type_check", sql`${table.dayType} IN ('regular', 'rest', 'holiday', 'vacation', 'leave', 'absence')`),
  check("fdp_time_entries_origin_check", sql`${table.origin} IN ('manual', 'import', 'integration')`),
  check("fdp_time_entries_minutes_check", sql`${table.expectedMinutes} >= 0 AND ${table.workedMinutes} >= 0 AND ${table.nightMinutes} >= 0 AND ${table.workedMinutes} <= 1440 AND ${table.expectedMinutes} <= 1440`),
  check("fdp_time_entries_night_check", sql`${table.nightMinutes} <= ${table.workedMinutes}`),
  check("fdp_time_entries_balance_check", sql`${table.balanceMinutes} = ${table.workedMinutes} - ${table.expectedMinutes}`),
  check("fdp_time_entries_punches_check", sql`jsonb_typeof(${table.punchesJson}) = 'array' AND jsonb_array_length(${table.punchesJson}) <= 12`),
]);

export const timeSheetEvents = pgTable("fdp_time_sheet_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  timeSheetId: text("time_sheet_id").notNull().references(() => timeSheets.id, { onDelete: "cascade" }),
  eventCode: text("event_code").notNull(),
  minutes: integer("minutes").notNull().default(0),
  source: text("source").notNull().default("computed"),
  justification: text("justification").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_time_sheet_events_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_time_sheet_events_sheet_event_uq").on(table.workspaceId, table.timeSheetId, table.eventCode),
  foreignKey({ name: "fdp_time_sheet_events_workspace_sheet_fk", columns: [table.workspaceId, table.timeSheetId], foreignColumns: [timeSheets.workspaceId, timeSheets.id] }).onDelete("cascade"),
  check("fdp_time_sheet_events_event_check", sql`${table.eventCode} IN ('overtime_50', 'overtime_100', 'night_hours', 'late_hours', 'absence_hours', 'interval_not_taken', 'on_call_hours', 'time_bank_credit', 'time_bank_debit')`),
  check("fdp_time_sheet_events_source_check", sql`${table.source} IN ('computed', 'manual')`),
  check("fdp_time_sheet_events_minutes_check", sql`${table.minutes} >= 0`),
  check("fdp_time_sheet_events_justification_check", sql`${table.source} <> 'manual' OR length(${table.justification}) >= 5`),
]);

export const timeInconsistencies = pgTable("fdp_time_inconsistencies", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  timeSheetId: text("time_sheet_id").notNull().references(() => timeSheets.id, { onDelete: "cascade" }),
  entryDate: date("entry_date", { mode: "string" }),
  kind: text("kind").notNull(),
  severity: text("severity").notNull(),
  detail: text("detail").notNull().default(""),
  status: text("status").notNull().default("open"),
  resolutionNote: text("resolution_note").notNull().default(""),
  resolvedBy: text("resolved_by"),
  resolvedAt: timestamp("resolved_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_time_inconsistencies_workspace_id_uq").on(table.workspaceId, table.id),
  // A data nula representa inconsistência da folha inteira; a coalescência mantém
  // a unicidade por (folha, dia, tipo) sem permitir duplicata do mesmo problema.
  uniqueIndex("fdp_time_inconsistencies_sheet_kind_uq").on(table.workspaceId, table.timeSheetId, sql`(coalesce(${table.entryDate}, DATE '1900-01-01'))`, table.kind),
  index("fdp_time_inconsistencies_workspace_status_idx").on(table.workspaceId, table.status, table.severity),
  foreignKey({ name: "fdp_time_inconsistencies_workspace_sheet_fk", columns: [table.workspaceId, table.timeSheetId], foreignColumns: [timeSheets.workspaceId, timeSheets.id] }).onDelete("cascade"),
  foreignKey({ name: "fdp_time_inconsistencies_workspace_resolver_fk", columns: [table.workspaceId, table.resolvedBy], foreignColumns: [workspaceMembers.workspaceId, workspaceMembers.userId] }),
  check("fdp_time_inconsistencies_kind_check", sql`${table.kind} IN ('missing_punch', 'odd_punch_count', 'duplicate_punch', 'out_of_order_punch', 'missing_schedule', 'future_date', 'work_on_non_working_day', 'overtime_without_justification', 'interval_below_minimum')`),
  check("fdp_time_inconsistencies_severity_check", sql`${table.severity} IN ('blocking', 'warning')`),
  check("fdp_time_inconsistencies_status_check", sql`${table.status} IN ('open', 'resolved', 'waived')`),
  check("fdp_time_inconsistencies_resolution_check", sql`${table.status} = 'open' OR length(${table.resolutionNote}) >= 5`),
]);

/**
 * Central de eventos de integração.
 *
 * Toda entrada vinda de fora — webhook do Teams, admissão lida da Sólides,
 * sincronização manual — passa por aqui antes de virar demanda. O índice único
 * em (workspace, integração, evento externo) é o que garante idempotência: o
 * segundo processamento do mesmo evento não insere, e portanto não duplica a
 * demanda. A garantia é do banco, não da boa vontade do conector.
 */
export const integrationEvents = pgTable("fdp_integration_events", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull(),
  connector: text("connector").notNull(),
  eventType: text("event_type").notNull(),
  externalEventId: text("external_event_id").notNull(),
  source: text("source").notNull().default("webhook"),
  payloadHash: text("payload_hash").notNull().default(""),
  payloadJson: jsonb("payload_json").notNull().default({}),
  status: text("status").notNull().default("received"),
  resultType: text("result_type").notNull().default(""),
  resultId: text("result_id").notNull().default(""),
  errorCode: text("error_code").notNull().default(""),
  errorMessage: text("error_message").notNull().default(""),
  retryCount: integer("retry_count").notNull().default(0),
  receivedAt: timestamp("received_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  processedAt: timestamp("processed_at", { withTimezone: true, mode: "string" }),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_integration_events_idempotency_uq").on(table.workspaceId, table.integrationId, table.externalEventId),
  uniqueIndex("fdp_integration_events_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_integration_events_workspace_status_idx").on(table.workspaceId, table.status, table.receivedAt),
  index("fdp_integration_events_workspace_connector_idx").on(table.workspaceId, table.connector, table.eventType, table.receivedAt),
  foreignKey({ name: "fdp_integration_events_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  check("fdp_integration_events_status_check", sql`${table.status} IN ('received', 'processing', 'processed', 'ignored', 'error', 'reprocessed')`),
  check("fdp_integration_events_source_check", sql`${table.source} IN ('webhook', 'polling', 'manual', 'retry')`),
  check("fdp_integration_events_retry_check", sql`${table.retryCount} >= 0 AND ${table.retryCount} <= 100`),
]);

/**
 * Movimentação reconhecida no Teams que ainda não vira demanda.
 *
 * Existe por causa de um requisito explícito: mensagem com dado insuficiente não
 * pode abrir demanda errada. Ela vira sugestão, e alguém do DP confirma,
 * completa ou rejeita. O texto original fica guardado para essa conferência.
 */
export const movementSuggestions = pgTable("fdp_movement_suggestions", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }),
  integrationId: text("integration_id").notNull(),
  eventId: text("event_id").notNull().references(() => integrationEvents.id, { onDelete: "cascade" }),
  movementKind: text("movement_kind").notNull(),
  status: text("status").notNull().default("pending"),
  confidence: integer("confidence").notNull().default(0),
  employeeName: text("employee_name").notNull().default(""),
  employeeId: text("employee_id"),
  previousSalaryCents: bigint("previous_salary_cents", { mode: "number" }),
  newSalaryCents: bigint("new_salary_cents", { mode: "number" }),
  previousRole: text("previous_role").notNull().default(""),
  newRole: text("new_role").notNull().default(""),
  effectiveDate: date("effective_date"),
  requestedByName: text("requested_by_name").notNull().default(""),
  teamId: text("team_id").notNull().default(""),
  teamName: text("team_name").notNull().default(""),
  channelId: text("channel_id").notNull().default(""),
  channelName: text("channel_name").notNull().default(""),
  messageId: text("message_id").notNull().default(""),
  messageUrl: text("message_url").notNull().default(""),
  originalMessage: text("original_message").notNull().default(""),
  missingFieldsJson: jsonb("missing_fields_json").notNull().default([]),
  signalsJson: jsonb("signals_json").notNull().default([]),
  cardId: text("card_id"),
  resolvedBy: text("resolved_by"),
  resolvedAt: timestamp("resolved_at", { withTimezone: true, mode: "string" }),
  resolutionNote: text("resolution_note").notNull().default(""),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_movement_suggestions_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_movement_suggestions_workspace_status_idx").on(table.workspaceId, table.status, table.createdAt),
  // Mensagem editada reencontra a própria sugestão em vez de abrir outra.
  uniqueIndex("fdp_movement_suggestions_message_uq").on(table.workspaceId, table.integrationId, table.messageId)
    .where(sql`${table.messageId} <> '' AND ${table.status} = 'pending'`),
  foreignKey({ name: "fdp_movement_suggestions_workspace_integration_fk", columns: [table.workspaceId, table.integrationId], foreignColumns: [integrations.workspaceId, integrations.id] }).onDelete("cascade"),
  check("fdp_movement_suggestions_kind_check", sql`${table.movementKind} IN ('salary_change', 'role_change')`),
  check("fdp_movement_suggestions_status_check", sql`${table.status} IN ('pending', 'confirmed', 'rejected', 'superseded')`),
  check("fdp_movement_suggestions_confidence_check", sql`${table.confidence} BETWEEN 0 AND 100`),
  check("fdp_movement_suggestions_resolution_check", sql`(${table.status} = 'confirmed' AND ${table.cardId} IS NOT NULL AND ${table.resolvedAt} IS NOT NULL) OR (${table.status} = 'rejected' AND ${table.resolvedAt} IS NOT NULL) OR (${table.status} IN ('pending', 'superseded'))`),
]);

/**
 * Controle de EPI (§ Controle de EPI).
 *
 * O módulo tem oito tabelas porque cada etapa da jornada do equipamento tem
 * campos próprios e prazo próprio: cadastro, entrega, devolução, troca por
 * dano, descarte e análise de desconto. O que as amarra é `fdp_epi_movements`,
 * o razão append-only que a aba do colaborador e os relatórios leem.
 *
 * `epi_condition` em vez de `condition`: a segunda é palavra reservada do
 * PostgreSQL, e `tests/sql-reserved-identifiers.test.mts` existe justamente
 * porque uma delas já derrubou uma varredura inteira em produção.
 */
export const stockLocations = pgTable("fdp_stock_locations", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  code: text("code").notNull(), name: text("name").notNull(), description: text("description").notNull().default(""),
  status: text("status").notNull().default("active"), isDefault: integer("is_default").notNull().default(0),
  createdBy: text("created_by").notNull(), updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_stock_locations_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_stock_locations_workspace_code_uq").on(table.workspaceId, table.code),
  uniqueIndex("fdp_stock_locations_workspace_default_uq").on(table.workspaceId).where(sql`${table.isDefault} = 1`),
  index("fdp_stock_locations_workspace_status_idx").on(table.workspaceId, table.status, table.name),
  check("fdp_stock_locations_status_check", sql`${table.status} IN ('active', 'inactive')`),
]);

export const epiProducts = pgTable("fdp_epi_products", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id"),
  name: text("name").notNull(),
  epiType: text("epi_type").notNull(),
  caNumber: text("ca_number").notNull(),
  size: text("size").notNull(),
  brand: text("brand").notNull(),
  model: text("model").notNull(),
  unitValue: numeric("unit_value", { precision: 14, scale: 2 }).notNull().default("0"),
  stockQuantity: integer("stock_quantity").notNull().default(0),
  registeredOn: date("registered_on", { mode: "string" }).notNull(),
  status: text("status").notNull().default("in_stock"),
  registrationReason: text("registration_reason").notNull(),
  notes: text("notes").notNull().default(""),
  productExpiresOn: date("product_expires_on", { mode: "string" }),
  caExpiresOn: date("ca_expires_on", { mode: "string" }),
  supplier: text("supplier").notNull().default(""),
  internalCode: text("internal_code").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_products_workspace_id_uq").on(table.workspaceId, table.id),
  uniqueIndex("fdp_epi_products_internal_code_workspace_uq").on(table.workspaceId, table.internalCode).where(sql`${table.internalCode} <> ''`),
  index("fdp_epi_products_workspace_status_idx").on(table.workspaceId, table.status, table.name),
  index("fdp_epi_products_workspace_company_name_idx").on(table.workspaceId, table.companyId, table.name),
  index("fdp_epi_products_workspace_ca_idx").on(table.workspaceId, table.caNumber),
  foreignKey({ name: "fdp_epi_products_workspace_purchasing_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }),
  check("fdp_epi_products_type_check", sql`${table.epiType} IN ('head', 'eye_face', 'hearing', 'respiratory', 'trunk', 'upper_limbs', 'lower_limbs', 'full_body', 'fall_protection', 'other')`),
  check("fdp_epi_products_status_check", sql`${table.status} IN ('active', 'inactive', 'in_stock', 'delivered', 'returned', 'sanitizing', 'discarded', 'damaged', 'lost')`),
  check("fdp_epi_products_reason_check", sql`${table.registrationReason} IN ('first_delivery', 'periodic_exchange', 'damage_replacement', 'loss_replacement', 'expiry_replacement', 'initial_purchase', 'stock_replenishment', 'manual_adjustment', 'other')`),
  check("fdp_epi_products_stock_check", sql`${table.stockQuantity} >= 0`),
  check("fdp_epi_products_value_check", sql`${table.unitValue} >= 0`),
]);

export const stockBalances = pgTable("fdp_stock_balances", {
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault),
  productId: text("product_id").notNull(), stockLocationId: text("stock_location_id").notNull(),
  quantity: integer("quantity").notNull().default(0), version: integer("version").notNull().default(0),
  updatedBy: text("updated_by").notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  primaryKey({ columns: [table.workspaceId, table.productId, table.stockLocationId] }),
  index("fdp_stock_balances_workspace_location_idx").on(table.workspaceId, table.stockLocationId, table.productId),
  index("fdp_stock_balances_workspace_product_idx").on(table.workspaceId, table.productId),
  foreignKey({ name: "fdp_stock_balances_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_stock_balances_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  check("fdp_stock_balances_quantity_check", sql`${table.quantity} >= 0`),
]);

export const epiDeliveries = pgTable("fdp_epi_deliveries", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  productId: text("product_id").notNull(),
  stockLocationId: text("stock_location_id").notNull(),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  employeeId: text("employee_id").notNull(),
  deliveredOn: date("delivered_on", { mode: "string" }).notNull(),
  positionName: text("position_name").notNull().default(""),
  departmentName: text("department_name").notNull().default(""),
  caNumber: text("ca_number").notNull().default(""),
  size: text("size").notNull().default(""),
  quantity: integer("quantity").notNull(),
  settledQuantity: integer("settled_quantity").notNull().default(0),
  unitValue: numeric("unit_value", { precision: 14, scale: 2 }).notNull().default("0"),
  responsibleUserId: text("responsible_user_id"),
  deliveryReason: text("delivery_reason").notNull(),
  status: text("status").notNull().default("delivered"),
  signatureName: text("signature_name").notNull().default(""),
  signedAt: timestamp("signed_at", { withTimezone: true, mode: "string" }),
  canceledReason: text("canceled_reason").notNull().default(""),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_deliveries_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_deliveries_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.deliveredOn),
  index("fdp_epi_deliveries_workspace_status_idx").on(table.workspaceId, table.status, table.deliveredOn),
  index("fdp_epi_deliveries_workspace_product_idx").on(table.workspaceId, table.productId),
  foreignKey({ name: "fdp_epi_deliveries_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_deliveries_stock_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  uniqueIndex("fdp_epi_deliveries_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_deliveries_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  check("fdp_epi_deliveries_quantity_check", sql`${table.quantity} > 0`),
  check("fdp_epi_deliveries_settled_check", sql`${table.settledQuantity} >= 0 AND ${table.settledQuantity} <= ${table.quantity}`),
  check("fdp_epi_deliveries_status_check", sql`${table.status} IN ('delivered', 'pending_signature', 'signed', 'canceled')`),
  check("fdp_epi_deliveries_reason_check", sql`${table.deliveryReason} IN ('first_delivery', 'periodic_exchange', 'damage_replacement', 'loss_replacement', 'expiry_replacement', 'initial_purchase', 'stock_replenishment', 'manual_adjustment', 'other')`),
  check("fdp_epi_deliveries_value_check", sql`${table.unitValue} >= 0`),
]);

export const epiDisposals = pgTable("fdp_epi_disposals", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  productId: text("product_id").notNull(),
  stockLocationId: text("stock_location_id").notNull(),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  employeeId: text("employee_id"),
  disposalDate: date("disposal_date", { mode: "string" }).notNull(),
  quantity: integer("quantity").notNull(),
  caNumber: text("ca_number").notNull().default(""),
  size: text("size").notNull().default(""),
  disposalReason: text("disposal_reason").notNull(),
  responsibleUserId: text("responsible_user_id"),
  status: text("status").notNull().default("awaiting_disposal"),
  confirmedBy: text("confirmed_by"),
  confirmedAt: timestamp("confirmed_at", { withTimezone: true, mode: "string" }),
  originType: text("origin_type").notNull().default("manual"),
  originId: text("origin_id"),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_disposals_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_disposals_workspace_status_idx").on(table.workspaceId, table.status, table.disposalDate),
  index("fdp_epi_disposals_workspace_product_idx").on(table.workspaceId, table.productId),
  foreignKey({ name: "fdp_epi_disposals_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_disposals_stock_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  uniqueIndex("fdp_epi_disposals_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_disposals_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  check("fdp_epi_disposals_quantity_check", sql`${table.quantity} > 0`),
  check("fdp_epi_disposals_reason_check", sql`${table.disposalReason} IN ('damage', 'contamination', 'natural_wear', 'expired', 'inadequate', 'unusable_return', 'mandatory_exchange', 'other')`),
  check("fdp_epi_disposals_status_check", sql`${table.status} IN ('awaiting_disposal', 'disposed', 'canceled', 'reevaluated')`),
  check("fdp_epi_disposals_origin_check", sql`${table.originType} IN ('manual', 'return', 'damage')`),
  check("fdp_epi_disposals_confirmation_check", sql`${table.status} <> 'disposed' OR (${table.confirmedBy} IS NOT NULL AND ${table.confirmedAt} IS NOT NULL)`),
]);

export const epiDiscountRequests = pgTable("fdp_epi_discount_requests", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  employeeId: text("employee_id").notNull(),
  productId: text("product_id").notNull(),
  deliveryId: text("delivery_id"),
  cardId: text("card_id"),
  requesterAreaId: text("requester_area_id"),
  responsibleAreaId: text("responsible_area_id"),
  competence: text("competence").notNull().default(""),
  movementId: text("movement_id"),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  title: text("title").notNull(),
  caNumber: text("ca_number").notNull().default(""),
  size: text("size").notNull().default(""),
  quantity: integer("quantity").notNull().default(1),
  unitValue: numeric("unit_value", { precision: 14, scale: 2 }).notNull().default("0"),
  totalValue: numeric("total_value", { precision: 14, scale: 2 }).notNull().default("0"),
  deliveredOn: date("delivered_on", { mode: "string" }),
  occurredOn: date("occurred_on", { mode: "string" }).notNull(),
  triggerReason: text("trigger_reason").notNull(),
  reasonNote: text("reason_note").notNull().default(""),
  analystUserId: text("analyst_user_id"),
  status: text("status").notNull().default("awaiting_dp_analysis"),
  decision: text("decision").notNull().default(""),
  decidedValue: numeric("decided_value", { precision: 14, scale: 2 }).notNull().default("0"),
  decidedBy: text("decided_by"),
  decidedAt: timestamp("decided_at", { withTimezone: true, mode: "string" }),
  decisionComment: text("decision_comment").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_discounts_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_discounts_workspace_status_idx").on(table.workspaceId, table.status, table.occurredOn),
  index("fdp_epi_discounts_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.occurredOn),
  index("fdp_epi_discounts_workspace_card_idx").on(table.workspaceId, table.cardId),
  foreignKey({ name: "fdp_epi_discounts_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_discounts_requester_area_fk", columns: [table.workspaceId, table.requesterAreaId], foreignColumns: [areas.workspaceId, areas.id] }),
  foreignKey({ name: "fdp_epi_discounts_responsible_area_fk", columns: [table.workspaceId, table.responsibleAreaId], foreignColumns: [areas.workspaceId, areas.id] }),
  foreignKey({ name: "fdp_epi_discounts_movement_fk", columns: [table.workspaceId, table.movementId], foreignColumns: [employeeMovements.workspaceId, employeeMovements.id] }),
  uniqueIndex("fdp_epi_discounts_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_discounts_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_epi_discounts_delivery_fk", columns: [table.workspaceId, table.deliveryId], foreignColumns: [epiDeliveries.workspaceId, epiDeliveries.id] }),
  foreignKey({ name: "fdp_epi_discounts_card_fk", columns: [table.workspaceId, table.cardId], foreignColumns: [cards.workspaceId, cards.id] }),
  check("fdp_epi_discounts_quantity_check", sql`${table.quantity} > 0`),
  check("fdp_epi_discounts_value_check", sql`${table.unitValue} >= 0 AND ${table.totalValue} >= 0 AND ${table.decidedValue} >= 0`),
  check("fdp_epi_discounts_trigger_check", sql`${table.triggerReason} IN ('lost', 'not_returned', 'misuse_damage', 'refused_return', 'manual_request', 'delivery_return_divergence')`),
  check("fdp_epi_discounts_status_check", sql`${table.status} IN ('awaiting_dp_analysis', 'awaiting_approval', 'approved_for_discount', 'refused', 'canceled', 'finished')`),
  check("fdp_epi_discounts_decision_check", sql`${table.decision} IN ('', 'no_discount', 'full_discount', 'partial_discount', 'request_more_info', 'forward_to_payroll', 'close_without_action')`),
  check("fdp_epi_discounts_decided_bound_check", sql`${table.decidedValue} <= ${table.totalValue}`),
  check("fdp_epi_discounts_approval_check", sql`${table.status} <> 'approved_for_discount' OR ${table.decidedValue} > 0`),
]);

export const epiReturns = pgTable("fdp_epi_returns", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  deliveryId: text("delivery_id").notNull(),
  productId: text("product_id").notNull(),
  stockLocationId: text("stock_location_id").notNull(),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  employeeId: text("employee_id").notNull(),
  returnedOn: date("returned_on", { mode: "string" }).notNull(),
  quantity: integer("quantity").notNull(),
  caNumber: text("ca_number").notNull().default(""),
  size: text("size").notNull().default(""),
  epiCondition: text("epi_condition").notNull(),
  receivedBy: text("received_by"),
  needsSanitizing: integer("needs_sanitizing").notNull().default(0),
  backToStock: integer("back_to_stock").notNull().default(0),
  sendToDisposal: integer("send_to_disposal").notNull().default(0),
  generateDpDemand: integer("generate_dp_demand").notNull().default(0),
  discountRequestId: text("discount_request_id"),
  disposalId: text("disposal_id"),
  sanitizationStatus: text("sanitization_status").notNull().default("not_required"),
  sanitizationStartedAt: timestamp("sanitization_started_at", { withTimezone: true, mode: "string" }),
  sanitizationCompletedAt: timestamp("sanitization_completed_at", { withTimezone: true, mode: "string" }),
  sanitizationResponsibleId: text("sanitization_responsible_id"),
  sanitizationResult: text("sanitization_result").notNull().default(""),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_returns_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_returns_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.returnedOn),
  index("fdp_epi_returns_workspace_condition_idx").on(table.workspaceId, table.epiCondition, table.returnedOn),
  foreignKey({ name: "fdp_epi_returns_delivery_fk", columns: [table.workspaceId, table.deliveryId], foreignColumns: [epiDeliveries.workspaceId, epiDeliveries.id] }),
  foreignKey({ name: "fdp_epi_returns_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_returns_stock_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  uniqueIndex("fdp_epi_returns_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_returns_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_epi_returns_discount_fk", columns: [table.workspaceId, table.discountRequestId], foreignColumns: [epiDiscountRequests.workspaceId, epiDiscountRequests.id] }),
  foreignKey({ name: "fdp_epi_returns_disposal_fk", columns: [table.workspaceId, table.disposalId], foreignColumns: [epiDisposals.workspaceId, epiDisposals.id] }),
  check("fdp_epi_returns_quantity_check", sql`${table.quantity} > 0`),
  check("fdp_epi_returns_condition_check", sql`${table.epiCondition} IN ('returned_sanitized', 'returned_pending_sanitizing', 'returned_damaged', 'returned_unusable', 'returned_for_disposal', 'not_returned', 'lost')`),
  check("fdp_epi_returns_flags_check", sql`${table.needsSanitizing} IN (0, 1) AND ${table.backToStock} IN (0, 1) AND ${table.sendToDisposal} IN (0, 1) AND ${table.generateDpDemand} IN (0, 1)`),
  check("fdp_epi_returns_missing_check", sql`${table.epiCondition} NOT IN ('not_returned', 'lost') OR ${table.backToStock} = 0`),
  check("fdp_epi_returns_sanitization_status_check", sql`${table.sanitizationStatus} IN ('not_required', 'awaiting', 'in_progress', 'sanitized', 'rejected')`),
]);

export const epiDamages = pgTable("fdp_epi_damages", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  employeeId: text("employee_id").notNull(),
  productId: text("product_id").notNull(),
  stockLocationId: text("stock_location_id"),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  deliveryId: text("delivery_id"),
  occurredOn: date("occurred_on", { mode: "string" }).notNull(),
  quantity: integer("quantity").notNull().default(1),
  damageReason: text("damage_reason").notNull(),
  description: text("description").notNull(),
  analystUserId: text("analyst_user_id"),
  decision: text("decision").notNull(),
  replacementProductId: text("replacement_product_id"),
  replacementCaNumber: text("replacement_ca_number").notNull().default(""),
  replacementSize: text("replacement_size").notNull().default(""),
  replacementQuantity: integer("replacement_quantity").notNull().default(0),
  replacementDeliveryId: text("replacement_delivery_id"),
  disposalId: text("disposal_id"),
  discountRequestId: text("discount_request_id"),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  updatedBy: text("updated_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_damages_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_damages_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.occurredOn),
  index("fdp_epi_damages_workspace_decision_idx").on(table.workspaceId, table.decision, table.occurredOn),
  foreignKey({ name: "fdp_epi_damages_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_damages_stock_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  uniqueIndex("fdp_epi_damages_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_damages_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  foreignKey({ name: "fdp_epi_damages_delivery_fk", columns: [table.workspaceId, table.deliveryId], foreignColumns: [epiDeliveries.workspaceId, epiDeliveries.id] }),
  foreignKey({ name: "fdp_epi_damages_discount_fk", columns: [table.workspaceId, table.discountRequestId], foreignColumns: [epiDiscountRequests.workspaceId, epiDiscountRequests.id] }),
  foreignKey({ name: "fdp_epi_damages_disposal_fk", columns: [table.workspaceId, table.disposalId], foreignColumns: [epiDisposals.workspaceId, epiDisposals.id] }),
  foreignKey({ name: "fdp_epi_damages_replacement_delivery_fk", columns: [table.workspaceId, table.replacementDeliveryId], foreignColumns: [epiDeliveries.workspaceId, epiDeliveries.id] }),
  check("fdp_epi_damages_quantity_check", sql`${table.quantity} > 0 AND ${table.replacementQuantity} >= 0`),
  check("fdp_epi_damages_description_check", sql`length(${table.description}) > 0`),
  check("fdp_epi_damages_reason_check", sql`${table.damageReason} IN ('natural_wear', 'work_accident', 'misuse', 'bad_storage', 'protection_loss', 'torn_broken', 'contaminated', 'soaked', 'other')`),
  check("fdp_epi_damages_decision_check", sql`${table.decision} IN ('exchange_without_discount', 'send_to_discount_analysis', 'refuse_exchange', 'send_to_disposal', 'send_to_sanitizing')`),
]);

export const epiMovements = pgTable("fdp_epi_movements", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  movementDate: date("movement_date", { mode: "string" }).notNull(),
  movementType: text("movement_type").notNull(),
  cnpj: text("cnpj").notNull().default(""),
  productId: text("product_id").notNull(),
  stockLocationId: text("stock_location_id"),
  targetStockLocationId: text("target_stock_location_id"),
  idempotencyKey: text("idempotency_key").notNull().default(""),
  epiName: text("epi_name").notNull(),
  caNumber: text("ca_number").notNull().default(""),
  size: text("size").notNull().default(""),
  employeeId: text("employee_id"),
  employeeName: text("employee_name").notNull().default(""),
  quantity: integer("quantity").notNull().default(0),
  stockDelta: integer("stock_delta").notNull().default(0),
  unitValue: numeric("unit_value", { precision: 14, scale: 2 }).notNull().default("0"),
  movementReason: text("movement_reason").notNull().default(""),
  epiCondition: text("epi_condition").notNull().default(""),
  status: text("status").notNull().default(""),
  generateDpDemand: integer("generate_dp_demand").notNull().default(0),
  demandId: text("demand_id"),
  discountRequestId: text("discount_request_id"),
  sourceType: text("source_type").notNull().default("product"),
  sourceId: text("source_id").notNull().default(""),
  responsibleId: text("responsible_id"),
  attachmentsJson: jsonb("attachments_json").$type<string[]>().notNull().default([]),
  notes: text("notes").notNull().default(""),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_movements_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_movements_workspace_employee_idx").on(table.workspaceId, table.employeeId, table.movementDate),
  index("fdp_epi_movements_workspace_product_idx").on(table.workspaceId, table.productId, table.movementDate),
  index("fdp_epi_movements_workspace_type_idx").on(table.workspaceId, table.movementType, table.movementDate),
  index("fdp_epi_movements_workspace_company_date_idx").on(table.workspaceId, table.companyId, table.movementDate),
  foreignKey({ name: "fdp_epi_movements_product_fk", columns: [table.workspaceId, table.productId], foreignColumns: [epiProducts.workspaceId, epiProducts.id] }),
  foreignKey({ name: "fdp_epi_movements_stock_location_fk", columns: [table.workspaceId, table.stockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  foreignKey({ name: "fdp_epi_movements_target_stock_location_fk", columns: [table.workspaceId, table.targetStockLocationId], foreignColumns: [stockLocations.workspaceId, stockLocations.id] }),
  uniqueIndex("fdp_epi_movements_idempotency_uq").on(table.workspaceId, table.idempotencyKey).where(sql`${table.idempotencyKey} <> ''`),
  foreignKey({ name: "fdp_epi_movements_employee_fk", columns: [table.workspaceId, table.companyId, table.employeeId], foreignColumns: [employees.workspaceId, employees.companyId, employees.id] }),
  check("fdp_epi_movements_type_check", sql`${table.movementType} IN ('registration', 'stock_entry', 'stock_transfer', 'delivery', 'exchange', 'return', 'sanitizing', 'sanitization_completed', 'disposal', 'discount_analysis', 'manual_adjustment')`),
  check("fdp_epi_movements_source_check", sql`${table.sourceType} IN ('product', 'entry', 'transfer', 'delivery', 'return', 'sanitization', 'damage', 'disposal', 'discount')`),
  check("fdp_epi_movements_flag_check", sql`${table.generateDpDemand} IN (0, 1)`),
  check("fdp_epi_movements_quantity_check", sql`${table.quantity} >= 0`),
]);

export const epiAttachments = pgTable("fdp_epi_attachments", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().default(tenantWorkspaceDefault).references(() => workspaces.id, { onDelete: "cascade" }),
  companyId: text("company_id").notNull(),
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  attachmentKind: text("attachment_kind").notNull().default("evidence"),
  objectKey: text("object_key").notNull(),
  filename: text("filename").notNull(),
  contentType: text("content_type").notNull(),
  sizeBytes: bigint("size_bytes", { mode: "number" }).notNull().default(0),
  createdBy: text("created_by").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "string" }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex("fdp_epi_attachments_workspace_id_uq").on(table.workspaceId, table.id),
  index("fdp_epi_attachments_entity_idx").on(table.workspaceId, table.entityType, table.entityId),
  foreignKey({ name: "fdp_epi_attachments_company_fk", columns: [table.workspaceId, table.companyId], foreignColumns: [companies.workspaceId, companies.id] }).onDelete("cascade"),
  check("fdp_epi_attachments_entity_check", sql`${table.entityType} IN ('product', 'delivery', 'return', 'damage', 'disposal', 'discount')`),
  check("fdp_epi_attachments_kind_check", sql`${table.attachmentKind} IN ('evidence', 'delivery_term', 'photo', 'document')`),
  check("fdp_epi_attachments_size_check", sql`${table.sizeBytes} >= 0`),
]);
